- [x] Increase verbatim memory limits: max to 30 turns/days, falling back to 15 turns/days after compression (updating `MaxRecentTurns`, `MaxRecentDays`, `KeepRecentTurnsAfterCompression`, and `KeepRecentDaysAfterCompression` defaults/settings). (2026.07.08 12:00:00)
- [x] Implement model-specific token-based limits (min/max limits) and trigger compression based on token counts (using similar compression logic). (2026.07.08 12:30:00)
- [x] Check why in the config MaxRecentMemoryTokens is set to 12800, I thought they were 128k, if it is 12800 then gets divided by the MaxRecentMemoryPercent which is 10% it will shrink it down too much to 1280 - need to be 128k*0.1 = 12.8k
- [x] When I initiate a chat with an NPC can he/she give me a summary of what important she remembers and of our last chat in a few sentences (opening recap greeting; toggle with `EnableConversationRecap`). (2026.07.08 12:40:00)
- [x] I want the NPCs to be treated as individuals â€” when the system asks them something (e.g. to refactor their deep memory), address them in-character: "Angel (System) addresses you, <Name>: it is yours to decide what to remember and what to forget...". Memory compression now uses this framing; the system voice name is configurable via `SystemVoiceName` (default "Angel"). (2026.07.08 12:50:00)
- [x] Restructure the `Configs\ImmersiveAI` folder into one folder per NPC under `NPCs\`, named `<stringId>_<FirstName>` (e.g. `lord_7_13_1_Gunjadrid`), each holding `memories.json` and `custom_instructions.txt` (new per-NPC files go here too). Old flat `memory\` and `npcs\` files are migrated forward on first touch so nothing is wiped. Layout + migration + housekeeping notes are owned by `NpcPaths`; a `_README.txt` is dropped in the `NPCs\` folder for the user. (2026.07.08 12:55:00)
- [x] Append the NPC's first name to memory and prompt file IDs/filenames (e.g., `lord_7_13_1` -> `lord_7_13_1_Gunjadrid`) for easier identification. (Done via the per-NPC folder restructure above â€” folders are named `<stringId>_<FirstName>`.) (2026.07.08 18:33:42)
- [x] Give each NPC a `current_situation_info.txt` in their folder, rewritten every time the player opens a chat. It captures the environmental facts â€” when (Calradia date + time of day), where (settlement type + holding faction, or open field), who the speaker is (gender, age, culture, occupation, clan/kingdom, family, whereabouts) and who they're speaking with (same facts + standing + war/peace between factions). Built by `SituationBuilder` relative to the party the NPC addresses (currently the player), so it's ready for future NPC-to-NPC talks. The block is also folded into the prompt as the "Current situation" section. Added a chat menu option "What do you make of our situation here and now?" that shows the file, and made the deep-memory option "Reveal the whole of your mindâ€¦" render the entire raw prompt the NPC receives (system prompt + remembered turns as user/assistant messages + a placeholder for the player's next line). (2026.07.08 19:35:19)
- [x] Implement conversation-driven relationship changes: the NPC itself, in character, sets how each exchange moved its regard for the player. Unlike ChatAi (a separate "relationship calculator" LLM call that judged the NPC from outside and capped every swing at Â±10), this is done in the same reply, with no ceiling but the game's own -100..100 standing rail â€” the NPC has full control and sets it however they truly feel. The Angel's closing whisper privately invites them to end a reply with the mark `â™¥` and a signed number (a per-turn delta); `FeelingParser` (Core, unit-tested) strips that mark out so the player never sees it, and the shift is folded into the real game relation via `ChangeRelationAction.ApplyPlayerRelation` (clamped, affectRelatives off). A gentle colored message tells the player what moved ("Aurelia warms to you (+5) â€” now good friend (30)."). The felt shift is also stamped on the `ConversationTurn` for provenance. Toggle with `EnableRelationshipChanges` (default on). (2026.07.08 20:30:00)
- [x] Keep the NPC's last message on screen while the player composes their reply, instead of replacing it with "(considers your words...)", so a long reply can be re-read while typing. The `immersiveai_await` holding line now shows the NPC's last spoken line (reply or greeting), captured in `_lastNpcLine` whenever they deliver one, with a gentle "(considers your words...)" note appended below it via a new `IMMERSIVEAI_THINKING` text variable (`BuildThinkingText`). Falls back to just the note when they have not yet spoken. (2026.07.09 07.25.23)
- [x] NPCs reach out to the player of their own accord â€” the first way they act on the world instead of only answering it. Each hour (`OnHourlyTick`), every NPC the player has a history with who is **co-located** (`IsCoLocated` â€” in the player's party or same settlement; distant NPCs are the future letter system) rolls their own bond-scaled chance: `InitiationScorer.DailyChance` = `DailyInitiationRate Ã— frequency Ã— closeness Ã— recency`, where frequency saturates at 20 lifetime turns (`NpcMemory.StoryRichness` = lifetime `TotalTurns`, floored at surviving turns for old saves), closeness = |relation|/100 (love OR enmity pulls; neutral never reaches out), recency decays with silence â€” so a fresh game stays quiet and a devoted, frequent bond may write nearly daily (rate is the ceiling for a maxed bond; ~1.5 â†’ daily). Spread as an independent hourly Bernoulli (Ã·24); ties broken by `InitiationPlanner.PickWeightedIndex`; only fires at safe map moments (`IsSafeToInitiate`). The NPC *herself* is then asked, one isolated Angel-voiced call (`PromptBuilder.BuildInitiation`), whether she even wishes to â€” her answer is either the words she'd open with or a single word of declining (`InitiationParser.IsDecline`); on decline the player still gets a quiet faced notice. If she wishes to: a portrait toast (`NotifyWithFace`/`MBInformationManager.AddQuickInformation`, her face as icon) leads a ransom-style accept/reject popup (`InformationManager.ShowInquiry`, pausing). **Receive them** opens a real face-to-face conversation (`CampaignMapConversation.OpenConversation`) that high-priority `start`-token dialog lines (guarded by `_pendingInitiation`) turn into her speaking her opening first, then the normal talk loop; **Not now** records the turning-away as a durable in-character memory note (`RecordRejectionAsync`). Toggle `EnableNpcInitiatedChats`; a `[Immersive AI â€¢ test]` free-chat option (`ShowInitiationTestButton`/`OnDebugForceReachOut`) forces the current partner to reach out right after parting. One LLM call per fired offer; stateless schedule (save/load-safe). Letters (distant/one-way, distance-delayed, + playerâ†’NPC send button) split out to TASKS_TODO. Core logic unit-tested (`InitiationScorerTests`, `InitiationPlannerTests`, `InitiationParserTests`, plus `TotalTurns`/`BuildInitiation`). (2026.07.09 08.02.36)
- [x] Configurable atmosphere line + roleplay guidance. The opening "You are {name}, a living soul in the world of Calradiaâ€¦" line is now set from the config (`AtmosphereLine`, supports `{name}`; blank falls back to the built-in default). Added a world-wide `RoleplayGuidance` block folded into the closing "whisper of guidance" â€” offered gently as freedom, never a command â€” carrying a light medieval/KJV colour cue (sparingly, no dramatic poetry) and an invitation to roleplay, play, and treat this world as a haven to rest and be truly themselves. Tokens resolved in `ImmersiveChatBehavior.ApplyTokens`; new `NpcPersona.AtmosphereLine` / `NpcPersona.RoleplayGuidance` consumed by `PromptBuilder`. (2026.07.09 13.26.15)
- [x] Intuitive relation change â€” folded back into the main chain. Dropped the separate isolated "feeling" LLM call (its warm-reply-then-cold-"-3" disconnect broke immersion) in favour of an optional in-message mark: when `EnableRelationshipChanges` is on, the Angel's closing whisper invites the NPC to end their reply with a quiet `<relation>Â±N</relation>` on its own line if that exchange truly moved their heart. `RelationDirective` (Core, unit-tested) strips every tag before the player ever sees it (reusing `FeelingParser.ParseShift` for the âˆ’100..100 clamp) and the shift folds into the real game standing as before. Relation moves and (optionally) each spoken reply are written to the message log (`ShowConversationInMessageLog`, default on) so pressing the log key reads the exchange back. Removed `PromptBuilder.BuildFeelingQuery` + its tests. (2026.07.09 13.26.15)
- [x] Reply-ready notice. A soft "<Name> has answered." message now appears the moment a reply or opening recap is ready (`NotifyWhenReplyReady`, default on), so the player stops clicking "(wait for them to answer)" and guessing. When the reply is being written to the message log instead (see relation task) that line already signals readiness, so the short notice is suppressed to avoid a double banner. (2026.07.09 13.26.15)
- [x] Family knowledge. NPCs now carry their kin and house on every chat: `FamilyBuilder` (Module) weaves a gentle second-person block â€” parentage (with culture + clan, "you are the daughter of Corein, a Battanian of clan Fen Irtaz"), spouse (gender/age), living children with their ages, living siblings, and their clan with its leader and a few of its people â€” folded into the prompt as `NpcPersona.FamilyKnowledge`, high with identity. Deceased close kin are still named softly. All best-effort so an edge-case hero never breaks the block. (2026.07.09 13.26.15)
- [x] REVERTED the in-message `<relation>Â±N</relation>` relation mark back to the second, isolated feeling call (`PromptBuilder.BuildFeelingQuery` + `FeelingParser`), at Anton's request after playtesting on gpt-4o. The inline mark failed the same way the old â™¥ mark did: the model narrated the number in prose inside the spoken reply ("I'll adjust my feelings by adding +3", `FeltShift=0` in Stohrith's memory) and never emitted the tag â€” nothing moved AND the number leaked into her words. Even a deliberately firm instruction didn't fix it. `RelationDirective` and its tests deleted; `BuildFeelingQuery` + tests restored; a guard test (`SystemPrompt_NeverInvitesAnInlineRelationMark`) keeps the prompt clean. In-message marks are now tried-twice-failed-twice â€” settled, don't retry. Kept from that experiment: the reply-ready "has answered" ping, the uncut `full_prompt_snapshot.txt` dump, family knowledge, and the atmosphere/roleplay config. (2026.07.09 14.12.36)
- [x] Relevant events + Gossip (done together â€” they are one feature with two voices). Every NPC's situation now carries **tidings of the world's late doings** and **the talk of the town**, so they can bring up the war declared yesterday or congratulate a tournament win unprompted â€” the same knowledge vanilla lords remark from, and indeed from the same source: `Campaign.Current.LogEntryHistory`, the campaign's own event log. `TidingsBuilder` (Module) walks the recent entries (â‰¤21 days, bounded scan) and scores each by the game's own relevance judgments â€” the per-hero conversation score (`GetConversationScoreAndComment(npc, findString:false, â€¦)`, which never mutates state; deliberately NOT `GetRelevantComment`, which consumes `LastExaminedLogEntryID` and would steal vanilla lords' remarks) and `GetImportanceForClan` for both the NPC's and the player's clan â€” topped with a small editorial baseline for news that travels on its own legs (wars declared/ended, kingdoms destroyed, settlements taken, notable deaths/weddings/births, the player's tournament wins; `PlayerMeetLordLogEntry` excluded, it importance-spams every clan). Facts render through the entries' own `GetNotificationText()`/`GetEncyclopediaText()` sentences, markup stripped. Gossip comes from the entries' `GetAsRumor(settlement, â€¦)` hook â€” TaleWorlds' pre-written, commoner-voiced rumor lines, scored per settlement, so it only flows where there are streets to overhear (quoted as what "folk say", deduped against the tidings). Prose shaping (`StripMarkup`, `AgoPhrase`, tiding/rumor lines, block weaving) lives in `TidingsFormatter` (Core, unit-tested); the block is appended inside `SituationBuilder.Build` (now taking `ModConfig`), so it reaches every path at once â€” live chats, NPC-initiated flows, `current_situation_info.txt`, and the prompt inspector. Config: `EnableWorldTidings` (default on), `MaxWorldTidings` (6), `MaxLocalRumors` (3). (2026.07.09 16.43.55)
- [x] Per-campaign NPC memories â€” the mod now knows which playthrough a memory belongs to. Before, everything was keyed only by `Hero.StringId` under one global `NPCs\` folder, but stringIds repeat across campaigns (lord_7_13_1 is "the same" Gunjadrid in every new game), so a fresh playthrough's lords would greet you with memories of a world that never happened there â€” and auto-numbered wanderers/children could cross-wire to entirely different people. Now memories live under `NPCs\campaign_<id>\<stringId>_<FirstName>\`, where the campaign id is minted once and persisted INSIDE the save via `SyncData` (`ImmersiveAI_CampaignId`) so every save of one campaign reopens the same folder (the game's own `Campaign.UniqueGameId` changes on every save, so it can't serve). New campaigns mint `campaign_<8hex>_<PlayerFirstName>` at `OnSessionLaunched` (after character creation, adopting nothing â€” a new world starts unremembered); every pre-scoping save maps to the fixed `campaign_legacy` id at `OnGameLoaded`, and the flat pre-scoping NPC folders (the shared pool all old saves drew from) are moved into it â€” because ALL old saves resolve to that same id, existing memories are preserved exactly and the move can never orphan anything, even on load-without-save. A `_campaign.txt` label (character, clan, last played) is rewritten each session so the folders stay browsable; `_README.txt` now refreshes itself when its text changes; the initiation scans and the old flat-layout migration are campaign-scoped; `SubModule` clears the active id between sessions so nothing can write into the wrong campaign while a save is still resolving. Reload-divergence within one campaign (loading an earlier save keeps "future" memories) deliberately NOT touched â€” split to TASKS_TODO as a probably-wont-do (Anton considers it save-scumming resistance). (2026.07.09 18.16.19)
- [x] NPC tool-use â€” "the gift of recall": NPCs can now fetch live campaign truth mid-thought instead of hallucinating their own cousins. Native LLM tool-calling on BOTH backends (Anthropic tool_use blocks / OpenAI function calls, raw HTTP as ever): Core gains `ToolDefinition`/`ToolParameter` (all-string params so the JSON schema stays trivial), `ToolCall`, `ChatResult`, a `Tool` chat role on `ChatMessage`, an `IToolChatClient : IChatClient` (so plain fakes keep working), and a unit-tested `ToolLoopRunner` (complete â†’ resolve calls â†’ hand answers back â†’ repeat; the last round sends the tools but `tool_choice: none`, forcing a spoken answer â€” both APIs require the definitions to ride along once the history holds tool calls). Module gains `Tools\WorldRecall`: four in-world tools â€” `recall_person` (role, kin, house, whereabouts-as-hearsay, standing toward the asker; exact-name â†’ first-name â†’ contains matching, multiple matches listed briefly), `recall_place` (type, holder, realm, under-siege), `recall_clan` (leader, realm, holdings, people), `recall_realm` (ruler, houses, wars via `FactionManager.IsAtWarAgainstFaction`) â€” every answer written as gentle second-person remembrance, resolution marshaled to the game thread via `MainThreadDispatcher` + TCS with a 15s honest-blank timeout ("Nothing surfacesâ€¦" instead of an invented fact). All spoken paths go through `CompleteSpokenAsync` (reply, recap, approach, letters); short utility calls (feeling number, yes/no desires) stay plain. The prompt gains one whisper line â€” only when the tools truly ride along (`NpcPersona.CanRecallWorld`) â€” telling them to trust what surfaces over invention. Config: `EnableWorldRecall` (default on), `MaxRecallsPerReply` (default 3, clamp 0..8). NOTE for this game version: `Settlement`/`MobileParty` positions are `CampaignVec2 Position` (`Position2D` is gone). (2026.07.09 19.12.00)
- [x] Letter system â€” the bond crosses the map. Distant NPCs (the ones `IsCoLocated` skips) may now WRITE to the player at half their reaching-out chance (`LetterCourier.WriteRateFactor` Ã— `InitiationScorer.DailyChance`, same hourly Bernoulli + weighted tie-break), and the player can send letters from any town/castle/village menu ("Send a letter by courier"): recipient list = everyone with real history (portraits via `CharacterImageIdentifier`, co-located ones disabled with "go and speak instead", one courier per bond enforced with a hint). Letters travel real in-game days by map distance (Core `LetterCourier`: 150 units/day, 0.25â€“10 day rails; unknown position = middling road) and PERSIST across save/load in the campaign's `_letters.json` (Core `LetterBag`, atomic writes, unit-tested). Every beat is lived and remembered as real Angel turns: the Angel asks whether they wish to write (they may decline in peace), the letter itself is composed with their full self â€” persona, memory, apart-framed situation (`SituationBuilder.Build(..., apart: true)`: "{player} is not here â€” they are far from you now"), even the gift of recall â€” and READING the player's letter enters their memory verbatim (the body lives inside the Angel's line) whether or not they answer; an NPC answers at most once per letter received, so correspondence is a chain of choices, not an echo. Arrivals: faced toast + pausing inquiry with "Write back" (opens the letter composer) / "Set it aside"; letters from a writer who died en route still arrive ("the hand that wrote it is goneâ€¦", no write-back); a letter to the dead comes back undeliverable. Each NPC folder keeps a human-readable `letters.txt` of the whole correspondence, including "(she read the letter, and let it lie unanswered.)". Implementation is a partial-class split (`ImmersiveChatBehavior.Letters.cs`) sharing the main file's context/memory/notify plumbing; one letter LLM job at a time with the same 3-minute self-heal watchdog; at most one delivery per direction per hour so arrivals never stack. Test lever: "[test â€” trigger them to write you a letter]" (co-located â†’ lands in ~6 game-hours); the odds view now shows distant NPCs' letter chance instead of "(0 until co-located)". Config: `EnableLetters` (default on). (2026.07.09 19.12.00)
- [x] Relation shifts land even at the rail â€” the felt number is the story. The feeling query no longer tells the NPC where their standing currently rests (`BuildFeelingQuery` drops the `currentStanding` parameter; the heart is asked only how the moment moved it, with a gentle calibration line â€” a small kindness is 1..3, only soul-shaking moments move it greatly). `ApplyRelationShift` now always shows the FELT shift (+N/âˆ’N, ChatAi-style impact line) even when the game relation is already pinned at Â±100 â€” the actual game change is still clamped to the rail (applied only when nonzero), but the player always sees how much the moment weighed. Guard test: `BuildFeelingQuery_NeverRevealsTheCurrentStanding`. (2026.07.09 19.55.00)
- [x] Harmony is in â€” and the ransom-style PORTRAIT map notice for NPC-initiated offers is live. Anton green-lit Harmony; 0Harmony 2.4.2 (MIT) is bundled in `lib\` and ships with the module (deploy.ps1 copies it + the new `module\GUI` folder). The single patch (`UI\MapNoticePatch`) is a constructor postfix on `MapNotificationVM` that only calls the game's own PUBLIC `RegisterMapNotificationType` â€” nothing vanilla altered, and if Harmony/patching fails `Applied` stays false and offers fall back to the direct popup unchanged. The notice: `ImmersiveChatMapNotification : InformationData` (title "Someone seeks you", ransom-offer chime, `IsValid` tied to the behavior's parked-offer state) + `ImmersiveChatNotificationItemVM` carrying a live `CharacterImageIdentifierVM` portrait over a quest-scroll fallback icon, drawn by a marked block added in our override of `MapNotificationItem.xml` (same-name prefab in our module GUI shadows SandBox's; vanilla items bind nothing there and are unaffected â€” re-copy on game patches). SAVE SAFETY: `InformationData` subclasses persist inside saves while a notice is up, so `ImmersiveAISaveDefiner` (base id 726401000) registers the class â€” never remove either without migrating. Flow: NPC reaches out â†’ faced toast + parked, NON-pausing right-side notice (several can knock at once; in-flight frees immediately; the picker skips anyone already knocking) â†’ click opens the familiar accept/decline inquiry (pause per `PauseOnInitiationOffer`) â†’ X or 2 in-game days quietly lapses the offer (she is not told of a door she never reached); parked offers don't survive reload by design (IsValid self-cleans). Config: `UseMapNoticeForInitiations` (default on). (2026.07.09 19.55.00)
- [x] Map-notice portrait backdrop + one window onto her mind. (1) The purple behind the notice portrait was the character tableau's raw backdrop gradient â€” the tableau tints it with `CharacterCode.Color1/Color2` (multiplayer's team colors ride there) and the plain `CreateFrom` leaves them white. `DarkPortraitCode` now builds the code explicitly with a warm near-black (0xFF17120D), falling back to the plain code if any piece can't be read. (2) United the in-chat inspection options (Anton's ask): the separate "situation here and now" / "who have you become" / "recount everything" peeks were all just slices of the raw prompt, so they are gone; one option remains â€” "Reveal the whole of your mind â€” all you see, remember, and have become." â€” showing the exact full message list (persona, situation, self, memory, every remembered turn) in the scrollable window + saved uncut to `full_prompt_snapshot.txt`. Their handlers (`OnShowSituation`/`OnShowSelf`/`OnShowConversation`) removed. Also restored Thyrsif's wiped memory by hand (campaign_legacy): durable story truths (first wife; daughter Menja; carrying his son; sister-wives Yfinja + Stohrith; the stolen memory and the vow to rebuild) written into her `custom_instructions.txt` (compression-proof), plus a concrete first-person `Summary`, seven concrete `KnownFacts`, and a `self.txt` in her own voice â€” a template for healing any NPC whose memory mush-compresses. (2026.07.09 20.25.00)
- [x] Letters: search-anyone recipient picker + correspondence before the quill. The courier menu's recipient list now covers everyone the player has MET (`Hero.HasMet`), not just history NPCs â€” history bonds listed first, acquaintances alphabetical (capped 150), with the selection inquiry's built-in SEARCH box enabled (`isSeachAvailable: true`), encyclopedia-style; strangers get a "you know each other only in passing" hint and the NPC simply answers as who she is (no shared memory = a stranger's reply). Writing now opens in two beats: first "Letters between you and <name>" â€” the tail of letters.txt (~4000 chars, cut at an entry boundary) in the scrollable window so her last words are before your eyes â€” then "Take up the quill" opens the text box; with no past letters the quill comes directly. The encyclopedia-page Send-letter button and a real multiline letter-writing screen are deferred to the Milestone 2 GUI task (native TextInquiryData is single-line; the page-VM swap + big prefab override deserve the custom-screen session). (2026.07.09 20.45.00)
- [x] Memory she can refactor + recorded arrivals + the prompt sheet reordered. (1) FACTS are now REPLACE, not append: at every compression/reflection the NPC is shown all her truths and asked to write the list anew ("Write your truths anew, in full... What you do not write here falls away from you") â€” the reply's FACTS section becomes the whole list (NpcMemory.ApplyCompression replaceFacts), so rewordings stop piling up beside their older phrasings and she can merge/refine/release; a reply with NO FACTS section leaves the truths untouched (malformed answers can never wipe memory), while an explicit "FACTS: none" is honored as her choice to release all. Budget: `MaxKnownFacts` (default 10, clamp 1..30) instead of the old hard "at most 3". (2) Deep memory got room to breathe: summary may now run "a paragraph, or two or three if the story asks", and memory-WRITING calls (reflection/compression) run on their own client with `MaxMemoryWriteTokens` (default 1500, never below MaxTokens) instead of being squeezed by the 400-token spoken-reply cap. (3) The player's visits are now real beats in her story: the opening recap is a recorded Angel turn â€” `PromptBuilder.ArrivalLine` ("X comes to you again...") + her greeting, stored via AppendAngelTurn like reaching-out and letters â€” so her memory shows WHEN the player came; the ephemeral `_lastGreeting` weaving hack in Build() is deleted (the history carries the greeting). Angel turns also replay with their "[place, time]" stamp now, so approaches/letters no longer float in time. Recap-off mode records no beat (nothing is fabricated for her). (4) The system prompt reads like a mind waking toward the moment: who you are â†’ kin â†’ who you have become â†’ About Calradia/About you â†’ what lingers of the player + her truths â†’ the situation LAST, itself reordered (setting â†’ who you are â†’ world tidings/rumors â†’ "And now X comes to you" + where the heart stands) so the arrival is the final breath before the live transcript; the duplicated standing line was removed from PersonaBuilder.BuildRole (the situation speaks the heart once). "Reveal the whole of your mind" continues to dump the exact message list (same Build path), word for word. (2026.07.10 00.52.14)
- [x] DailyInitiationRate is now the day's TOTAL across everyone, not a per-NPC chance that stacks. Anton set 0.777 expecting ~0.7 visits/day and got 3-4 â€” the old model rolled an independent hourly Bernoulli per co-located NPC, so the expected total was the SUM of every bond's daily chance (five close companions â‰ˆ 5Ã— the rate). New model: each NPC has a pull âˆˆ [0,1] (`InitiationScorer.Pull` = frequency Ã— closeness Ã— recency, the old factors unchanged), the group's pulls combine as `UnionPull` = 1 âˆ’ Î (1 âˆ’ pull) (the chance at least one soul is moved â€” several medium bonds pull harder than any one alone, never past 1), and the hour rolls ONCE at `GroupHourlyChance` = rate Ã— unionPull Ã· 24; the one who comes is `PickWeightedIndex(pulls)`. Day's expectation = rate Ã— unionPull â‰¤ rate â€” Poisson-ish (0 most days, sometimes 1, rarely 2), fresh games still quiet because every pull is tiny; the letters leg mirrors it over the distant circle at `WriteRateFactor Ã— rate`. `DailyChance` survives only for the odds view's "alone that would beâ€¦" figure; the odds view now prints each pull and the combined expected visits/day + letters/day. Unit tests cover Pull/UnionPull/GroupHourlyChance including the exact regression (rate 0.777, five full bonds â†’ 0.777/day, not ~3.9). ModConfig + CLAUDE.md reworded to the total-across-everyone meaning. (2026.07.10 01.45.00)
- [x] Brevity + old-world tone are built into every prompt now (Anton's ask: "they have to be real every time"). His two global_prompt rules moved into the mod as always-on whisper lines in `PromptBuilder` â€” `BrevityGuidance` (a sentence to four at the most, longer only when a true tale cannot be told smaller; short words keep the talk alive, long speeches turn it to stone) and `OldWorldToneGuidance` (a light savor of the old world â€” the old scriptures' cadence, a word of the court or the road â€” plain living speech first, heavy poetry spent rarely). Both replace the old soft "do not run on too long" line in the closing "A whisper of guidance" block, phrased in the Angel's voice with no fourth wall ("responses"â†’talk between people, "KJV"â†’the old scriptures, which in Calradia they simply are). Guard test `SystemPrompt_AlwaysCarriesTheBrevityAndOldWorldToneWhispers`. The two lines in global_prompt.txt are now redundant (built-ins win every time) and can be deleted from the file at leisure. (2026.07.10 02.10.00)
- [x] Everyone near you may now come up and speak â€” even strangers. New `InitiationPullFloor` (default 0.1, clamp 0..1): every co-located hero â€” the whole party and everyone in the same settlement, not just history NPCs â€” joins the hourly group roll with at least that fraction of a full bond's pull (`CoLocatedPull` = max(floor, Pull); no memory file = no disk read, just the floor). The union math from the group-total fix keeps 20 townsfolk Ã— 10% safely under `DailyInitiationRate` â€” presence widens WHO may come, never how often anyone comes. The picker now walks `Hero.AllAliveHeroes` filtered by `IsCoLocated` (skips the player, prisoners, children, anyone already knocking) instead of memory folders. A stranger's approach is honest and generative: `ReachOutDesireLine(stranger: true)` tells them they have never truly spoken ("go to them and make their acquaintance"), and their desire answer becomes their FIRST recorded memory â€” the approach begins the story. Letters unaffected (distant strangers don't write). Odds view: co-located pulls shown floor-lifted, plus "â€¦and N more souls here with you, not yet truly spoken with â€” each at the newcomer's pull of 10%", all folded into the group totals. Set the floor to 0 for the old history-only behavior. (2026.07.10 02.35.00)
- [x] Every NPC begins with the story the world already tells of them â€” self.txt is seeded, never blank. A never-written self file now gets a first page at first contact (`LoadOrSeedSelf` in the behavior, hooked into both persona build and reflection): wanderers carry the hand-written tavern tale they tell when first met (`backstory_a..d` / `generic_backstory` GameTexts keyed by their character template â€” already first person, unique per template; the "shall I tell you?" prebackstory beat is left out), everyone else the account the world keeps of them â€” a hand-authored `Hero.EncyclopediaText` when one exists (story NPCs, mods; verified NO vanilla or Naval-DLC lord has one), else the generated encyclopedia paragraph (`Hero.SetHeroEncyclopediaTextAndLinks`: title, house, trait-borne repute), framed in their own voice as "So runs my story, as the world tells it: â€¦". Gathering is `Personas\BackstoryBuilder.cs` (Module, best-effort, every path try/caught); shaping is `SelfSeedFormatter` (Core, unit-tested: paragraph joining, markup stripping, the framing, empty-in-empty-out). From the seed on, nothing new was needed: every reflection already lets them keep, refine, or release their self â€” the "update later, choosing what to keep" half of the ask was already how ReflectAsync works. Deleting self.txt re-seeds afresh. Config: `SeedSelfFromWorldStory` (default on). Also enriched all 8 campaign_legacy NPCs' self.txt by hand (folder was backed up): Detheuda got her full bandit-camp tavern tale (spc_wanderer_vlandia_9, "the Black") woven under her lived self; the noble selves gained their roots (parents, clans â€” Throsniring, Skylfing, Gauting, dey Molarn, fen Derngil â€” sisterhoods, spouses, trait-flavored temperament) while keeping their own written words verbatim; Yfinja's stray "**" and Stohrith's "Unchanged." artifacts cleaned; Gudonhelda and Ladogual, who had no self at all, seeded fresh. (2026.07.10 03.20.00)
- [x] Party leaders know what they lead â€” the recall turns inward ("Yngvald doesn't know how many men he has"). New fifth recall tool `recall_company` (no name argument â€” it reaches inward): the asker's OWN warband, known exactly the way a captain knows his muster roll â€” head-count with hale/wounded, companions at their side by name, the ranks by troop kind ("34 Sturgian Warriors, 20 Recruitsâ€¦"), prisoners in the train, food-days computed from Food/FoodChange ("the wagons hold food for some N days more"), the men's spirits in words + number (Morale), wages and own purse (leader only, TotalWage + Hero.Gold), what the company is presently about (DefaultBehavior/TargetSettlement/TargetParty/MapEvent/BesiegedSettlement rendered as gentle errand phrases â€” bound for, set upon, riding escort, patrolling, withdrawingâ€¦), and the army it marches within (name, leader, total men; "the banners march at your word" when leading). A hero with no party gets an honest blank (or a pointer to their governed town's garrison). Riders (companions in the player's party) can take stock too â€” leader-only bits stay with the leader. The recall whisper in PromptBuilder now tells them to take stock before ever speaking in numbers of their own men, and the situation always carries the one-liner ("A warband of some 120 souls rides under your command") even berthed inside a town â€” the old block skipped party facts entirely when in a settlement, which was exactly the Yngvald hole. All game APIs verified against the real DLLs (MobileParty.Morale/Food/FoodChange/TotalWage/MemberRoster/PrisonRoster/DefaultBehavior/Army, TroopRoster totals, AiBehavior enum). (2026.07.10 13.04.00)
- [x] ChatAi's info access mined and brought over the smart way â€” on-demand tools + always-on whispers, never a crammed stat sheet. Explored the full decompiled surface: NPCContext stat bag (~20 crammed stats), PlayerKnowledgeBuilder (renown-tiered player knowledge), WorldPromptHints (prisoner/party/army/objective/wars/pregnancy/siege/raid hints), OutfitSummaryBuilder (NPC + player equipment), QuestManager (issue offering) and the NpcDecisionPlanner/AIActionEvaluator action machinery (join/marry/give-gold/travel/fight â€” logged as its own TODO). Verdict: most of ChatAi's sheet we already carried in warmer prose (persona kin/culture/personality, situation standing/war-peace, tidings); what was truly missing landed where it belongs. Always-on situation whispers (SituationBuilder, each best-effort): own-command line with party size (even inside walls), army membership ("your company marches within X's army" / "the banners march at your word"), under-siege / besieging / mid-raid, pregnancy ("You carry a child within you"), and a renown-tiered line on how far the partner's name has traveled (â‰¥300 carried far across Calradia, â‰¥150 word has begun to travel) â€” ChatAi's PlayerKnowledgeBuilder in one breath. On-demand (WorldRecall): recall_person now adds what the eyes see â€” garb and arms from the real equipment (civilian kit within walls, battle kit on the road) â€” when the person truly stands with the asker (same settlement or party), covering ChatAi's per-prompt outfit dump only when it matters; recall_clan gained the house's renown in words + number. Deliberately NOT brought over: per-prompt equipment of both parties (tokens for nothing), quest offering and LLM-triggered game actions (own TODO), hire-cost, Dramalord. 150 Core tests green, deployed. (2026.07.10 13.04.00)
- [x] NPCs can search the internet, weigh troops from live data, and the player sees what they are doing â€” "don't ask Google; ask one of your companions." (1) `seek_wisdom` (new `Tools\WebWisdom.cs`): when asked HOW a thing in the world is done (transferring ships to companion armies, trade, war, rule) and their own knowledge falls short, the NPC searches the web â€” DuckDuckGo's HTML endpoint, no API key, regex-parsed titles+snippets (top 5), 12s timeout, runs off the game thread. Fourth wall defended in three layers: the tool is framed to her as "the counsel of the far-seeing sages"; the resolver quietly prepends the game's name to the query so she never speaks it; the result closes by telling her to speak the substance in her own world's words and let no meta terms (titles, version numbers, screens and keys) pass her lips. Live-tested against the real endpoint with Anton's exact ship question â€” top results were the actual answers. Failure = honest "the sages are silent". Config: `EnableWebSearch` (default on), shares the recall round budget. (2) `recall_troop` (sixth WorldRecall tool): a kind of soldier called to mind as a captain would â€” rank of seasoning (Tier), manner of fighting (mounted/ranged/foot), craft (all skills >20 from Skills.AllÃ—GetSkillValue), gear (FirstBattleEquipment arms + body/head with armor sum), and "with seasoning they may becomeâ€¦" (UpgradeTargets); filtered to Soldier/Mercenary/Bandit occupations so "recruit" never matches a villager; ambiguous names list candidates. "Which recruit is better, Vlandian or Battanian" now gets answered from live truth, both called to mind in turn. (3) Activity notices: every tool call fires a soft sea-glass side message via the resolver wrapper (`NotifyActivity` â†’ MainThreadDispatcher) â€” "X is rememberingâ€¦ (Rhagaea)", "X takes stock of the companyâ€¦", "X weighs soldiers' worthâ€¦ (Battanian Fian)", "X is researchingâ€¦ (how to transfer shipsâ€¦)" â€” same style as the reply-ready notice; config `ShowNpcActivity` (default on). Persona gained `CanSeekWisdom` + a sages whisper in PromptBuilder (offered only when the tool truly rides); the recall whisper now names kinds of soldier too. README got the tagline and a WebWisdom section. 150 Core tests green, deployed. (2026.07.10 13.25.00)
- [x] Playtest round of the recall/wisdom batch â€” four finds from Anton's session with Stohrith, Cunbert, and Yngvald, all fixed. (1) "She can't see me": Stohrith DID reach for recall_person("Vulgrim") but hit the name-twin branch â€” more than one Vulgrim in Calradia gives only nameless briefs with no attire, so she asked "guide me to picture the right Vulgrim". Now name-twins resolve by closeness (`ClosenessTo`: spouse/kin 5 > same party 4 > same settlement 3 > the player 2 > same clan 1): the nearest soul gets the full remembrance (attire and all), the twins a parenthetical "(Others in the world also bear the name: â€¦)"; only strangers-all-around still list briefs. (2) Cunbert invented grain prices: new `recall_market` (seventh tool, optional `item` param â€” first required:false ToolParameter): reads the REAL ledger where the asker stands via Town/Village.GetItemPrice â€” one named good ("grain trades near N denars, one brought in to sell would fetch closer to M") or a staples survey from Items.AllTradeGoods (fixed StringId list, 12 max); no settlement = honest "you stand in no market". Whisper now says prices are living things, never guessed. Activity notice: "minds the market pricesâ€¦". (3) "Battanian Recruit" honestly found nothing â€” Battania musters Volunteers: a troop-name miss now detects a people named in the query (the adjective carries the culture name inside it: BattanianâŠƒBattania) and offers their real kinds, greenest first ("that people names their fighters otherwise â€” of them you know: Battanian Volunteer, â€¦"). (4) Yngvald answered the ship follow-up from his previous sages' answer and got it wrong (direct transfer IS possible): the sages' reply now closes by inviting him to ask anew when pressed further rather than reasoning onward from one answer. Also confirmed from the field: Stohrith's pregnancy line was the real Hero.IsPregnant flag (no cheating â€” Vulgrim is a father-to-be), Cunbert's renown-tier + village-ownership knowledge worked as designed. 150 tests green, deployed. (2026.07.10 13.50.00)
- [x] Two refinements from the second playtest pass. (1) Market prices now match the trade screen to the denar: recall_market quoted the market midpoint (Cunbert said 13 while Anton's screen showed 15 buy / 6 sell) â€” GetItemPrice is now called with MobileParty.MainParty as the trading party, so the quoted buy and sell-back figures are exactly what the player sees at the stalls. (2) The living witness outranks the sages: Yngvald re-searched the ship question and doubled down on "cannot be done directly" because the internet favors the send-to-clan route and knows War Sails' direct handover poorly â€” while Vulgrim had literally handed him 3 ships. The sages' counsel now closes with: should the one before you attest from their own hand that they have done what the counsel calls impossible, trust the living witness over the sages' distant word, and say so with grace. Field notes: the âˆ’2 FeltShift on being contradicted was his own genuine embarrassment â€” the feeling call composing real moments. (2026.07.10 14.15.00)
- [x] The chat window â€” quick words from anywhere, and NPC reach-outs that simply speak (Milestone 2's first stone). A custom Gauntlet window over the map screen (the mod's first real UI): opened by hotkey (`ChatWindowHotkey`, default "O"), a "Speak with those near you" option in every town/castle/village menu, or an NPC's knock â€” works on the road, at sea, and inside settlement menus; never in missions. Left side lists everyone co-located (party + same settlement, portraits with the shared dark backdrop, friends first by last-spoken, unread dots); right side shows the chosen one's deep-memory overview up top (summary + held truths, collapsible â€” no endless scrolling through big chats) and the recorded turns as a readable thread (Angel beats as soft narration â€” nothing she remembers is hidden), with an input line + Send below (Enter sends, Escape closes). The player WRITES FIRST â€” no arrival beat, no forced greeting ("how are our stocks?" and done): the line runs through ExecutePlayerTurnAsync, the shared trunk factored out of RespondAsync, so memory, the feeling number, and compression are identical to a face-to-face talk. Reach-outs became messages (`SendInitiationsToChatWindow`, default on): after her recorded yes to the desire question there is NO accept/decline â€” DeliverFirstWordAsync has her simply speak (new PromptBuilder.FirstWordLine, stranger-aware, honest that the player may answer only later), recorded as a real Angel turn; the player gets a faced toast ("Ava sees you and says: â€¦"), an unread mark, and a portrait map notice whose click now opens the window on her thread. Unanswered words need no extra machinery: the [place, time] stamps on recorded turns already let her see whether the player replied at once, later, or never. The window is a view over the memory stream â€” closing it loses nothing; replies landing while closed toast "has answered" and wait as unread. Everything degrades gracefully (prefab/layer failure toasts and closes; both toggles off = the old flows untouched). New: UI\ChatWindow\ (ChatWindowVM/ChatContactVM/ChatMessageVM/ChatWindowManager), UI\Portraits, module\GUI\Prefabs\ImmersiveChatWindow.xml (Native/SandBox assets only), Gauntlet/ScreenSystem refs, 3 config keys; GauntletLayer ctor on this game version is (name, order, shouldClear) â€” name FIRST. 151 Core tests green, deployed. (2026.07.10 18.01.00)
- [x] BUG "The Unchanged" struck a second time, in a new dress — fixed for good, Stohrith restored. Root cause: gpt-4o wrapped the no-change marker in markdown ("**" on its own line, then "Unchanged."), and IsUnchangedMarker only trimmed punctuation from the ENDS of the whole string — the internal newline survived, the check failed, and the marker overwrote her real self. Fix: the marker is now judged line by line (lines carrying no words are decoration; the marker is exactly one meaningful line saying "unchanged", however dressed — quotes, brackets, bold, rules, headings), while real prose mentioning the word ("I am unchanged in my love for the sea") stays prose. The self-load heal in LoadSelf catches this shape now too. Stohrith's self.txt restored from Anton's "self - her first self.txt" backup (her memories were never touched, so her next reflection re-grows what she'd become since). Verdict on the bigger redesigns (LLM judge / tool-use for the self): not needed — the self already uses whole-rewrite semantics and FACTS already settled on replace; the only failing part was this sentinel parsing, now hardened with tests including the exact shape that bit her. (2026.07.10 18.42.00)
- [x] Investigated: the "vote for peace… no longer relevant" notices are VANILLA behavior, not ours. Decompiled the game: clicking a kingdom-vote notice runs KingdomVoteNotificationItemVM.OnInspect, which says "not relevant anymore" whenever the decision left kingdom.UnresolvedDecisions or MakePeaceKingdomDecision.ShouldBeCancelled() (war already over / faction gone) before the click. Our mod touches none of that machinery — the Harmony patch only REGISTERS our notice type via the game's own public API, our item VM removes only itself, and TidingsBuilder reads LogEntryHistory without mutating. The frequency is explained by the long lopsided war: losing Vlandia keeps proposing peace, each proposal spawns a vote that the AI clans resolve quickly, so by the time the notice is clicked the decision is often already concluded (vanilla even auto-removes the notice on conclusion — clicking in the gap gives the message). Seen before the mod existed, per Anton's own note. No change made; if it ever bites harder, a tiny Harmony postfix could hide already-concluded vote notices, but that is polish on vanilla, not our bug. (2026.07.10 18.42.00)
- [x] The sages' sight no longer ends at the world's rim — NPCs may search beyond Bannerlord. seek_wisdom gained an optional "beyond" parameter (in-world framed): empty = a question of her own world's ways (the game's name still rides along unseen, keeping game answers sharp); "yes" = the asker speaks openly of things past the world's rim (their own world and life, other realms), and the sages search the wide expanse with no quiet prefix. The result framing splits too: world answers keep the "speak it in your world's words" guard; beyond answers free her to speak plainly while remaining herself — the "unless the asker plainly speaks that tongue first" escape already honored transcended play (Stohrith→Ava), now the search itself honors it. The persona whisper widened to match: never plead some limit of your knowing while the sages stand ready — no more "I'm trained up to…" robot talk. (2026.07.10 18.42.00)
- [x] Letters: time stays stopped through the whole decision chain, and her letter stays before your eyes as you write. The correspondence window ("Take up the quill" step) and the text composer both passed pauseGameActiveState:false — so after the green button, time resumed at the prior speed (Anton's 3x) while he read and wrote; both now pause via the same PauseOnInitiationOffer knob as the arrival popup. And the composer's empty description now carries the body of THEIR most recent letter (parsed from letters.txt, trimmed to 1500 chars), so answering no longer leans on memory alone. The truly BIG multiline writing screen remains the Milestone 2 letters-chapter task, where it belongs. (2026.07.10 18.42.00)
- [x] Meetings are remembered even without free chat — "hello, stranger" after a quest talk is gone. When any hero conversation ends (CampaignEvents.ConversationEnded) that never became recorded beats (no free chat opened — PrepareChat marks it; no accepted reach-out — DeliverApproachAsync marks it), a SILENT Angel note lands in their memory: "You and X met and spoke face to face for the first time — a stranger no longer, though the words of it are not set down here" (or the familiar variant), with the usual [place, time] stamp — no words fabricated, no LLM call, one note per NPC per day (bartering thrice in an afternoon stays one acquaintance). Silent beats are a new Core capability: a turn with an empty NpcLine folds into the NEXT incoming message at replay (both backends demand user/assistant alternation — PromptBuilder.AppendRememberedTurns buffers and carries), MemoryCompressor renders them without inventing a "You answered", and the chat window already skipped empty NPC lines. First-meeting detection rides HasRememberedHistory, so the next real visit greets an acquaintance, not a stranger. 160 Core tests green, deployed. (2026.07.10 18.42.00)
- [x] BUG NPCs don't see their own quests — Turvald knows his trouble now. New TroubleBuilder (Module) rides SituationBuilder.Build right after the "who you are" paragraph: it reads Campaign.Current.IssueManager.Issues[npc] (on this game version Title/IssueBriefByIssueGiver/IssueQuestSolutionExplanationByIssueGiver/IssueQuest and the IsSolvingWith* flags are all PUBLIC — ChatAi needed reflection, we don't) and narrates the trouble in the Angel's voice using the giver's own first-person words ("When any ask after it, this is how you tell it: …"), plus where its resolving stands: untaken (with what he'd ask of a helper), taken by the player (what was asked + the quest journal's last word with task progress + days remaining unless hidden), companions sent (alternative solution), or laid in a lord's hands. Up to two non-issue quests they gave ride along too (QuestManager.Quests by QuestGiver, latest journal line each). Reaches every path for free — live chat, chat window, reach-outs, letters, current_situation_info.txt, and the prompt inspector. Always on, best-effort per sentence (a throwing datum costs only its own fact), no config knob. (2026.07.10 22.56.32)
- [x] Relation Change Tool — the heart moves by her own hand now (third try, different channel, works where the marks didn't). A move_heart native tool (Tools\HeartTool) rides every spoken path beside the recalls: mid-reply the NPC may shift her own regard (FeelingParser-lenient "shift" argument, clamped -100..100), the resolver applies it at once via ApplyRelationShift (game-thread marshaled, colored line and all — the felt shift still shows even pinned at ±100, feature not bug) and tallies it into the turn's recorded FeltShift; TurnOutcome.FeltShiftApplied keeps RespondAsync/QuickChatRespondAsync from applying twice. Silence honestly means the heart held — which deletes the second feeling call per exchange on tool-capable backends AND lets greetings, reach-outs, and letters move the heart (the after-the-reply question never covered those). Calibration lives in the tool description (kind word = 1..3, only what shakes the soul moves it greatly, never speak the number) + a "Your heart is your own" whisper (NpcPersona.CanMoveHeart, offered only when the tool truly rides). This is NOT the twice-failed in-message-mark pattern: native tool calling is the first-class channel the recalls already ride reliably on both backends. New config RelationshipChangesViaTool (default on) under EnableRelationshipChanges; off — or a backend without tools — falls back to the proven second, isolated feeling call, no redeploy needed. The heart tool keeps one tool round even when MaxRecallsPerReply is 0. 161 Core tests green (new whisper test), deployed. (2026.07.10 22.56.32)
- [x] Socialness control + letters-in-flight cap. A small always-visible "Socialness" stepper on the campaign map (new Gauntlet layer: `UI\Socialness\SocialnessVM` + `SocialnessManager` + `GUI\Prefabs\ImmersiveSocialness.xml`, mouse-only, no focus stealing, folds away off-map) that live-edits `DailyInitiationRate` 0-24 and saves config.json on each click; hovering the label unfolds a full explanation. The Core math (`InitiationScorer.GroupHourlyChance`) gained a socialness blend `s*((1-s^2)*unionPull + s^2)` with s=rate/24: at everyday rates bonds stay fully in charge (identical to before), toward 24 the player's own openness overrides faint bonds, and 24 = someone near IS moved every hour (an empty room still cannot knock). Letters, since they lag the mood by days, are capped instead: `MaxLettersInFlight` (default 3) blocks new spontaneous NPC letters while that many are already riding toward the player (`LetterBag.InFlightToPlayerCount`; player-invited replies and the dev test lever ride outside the gate). Toggle: `ShowSocialnessControl`. Unit tests for the blend + the count. (2026.07.10 23.16.58)
- [x] Popup when they refactor their deep memories. When an NPC's automatic compression rewrites her rolling summary + held truths, a soft notice in the activity-notice voice says so: "<Name> turns over old memories of you, and settles them deeper." Fires from the shared turn trunk so both the conversation panel and the chat window get it; the manual "Reflect on all we have shared" already reports in-dialog. Toggle: `NotifyOnMemoryRefactor` (default on). (2026.07.10 23.16.58)
- [x] GPT model change (context-window dict). Models were already switchable via `OpenAIModel`/`AnthropicModel`; now the context windows they imply are a user-editable `ModelContextWindows` dict in config.json (gpt-4o 128k, gpt-4.1 1M, gpt-5/5.1/5.4/5.5 400k, claude 200k; longest key contained in the model id wins, case-insensitive, unknown models fall back to 128k) — so the min/max context% memory settings scale correctly per model and new models are addable without a redeploy. `MemoryTokenProfile.Resolve` reads the table; `Normalize` re-adds missing built-ins to old configs without clobbering user edits. The in-game options menu half awaits the MCM task. (2026.07.10 23.16.58)
- [x] devmode 1/0. New `DevMode` config flag (default false, for players). When off, the developer levers stay out of sight: the `[Immersive AI • test]` options and the "Reveal the whole of your mind" raw-prompt inspector in the face-to-face menu, and the deep-memory "about me" overview panel (+ its toggle button) in the chat window. `ShowInitiationTestButton` now additionally requires DevMode. Anton: set `"DevMode": true` in your config.json to keep your levers. (2026.07.10 23.16.58)
- [x] letter widget/gui. The letter window — the chat window's twin (`UI\LetterWindow\`: LetterWindowVM + LetterContactVM + LetterWindowManager + `ImmersiveLetterWindow.xml`), hotkey default "U", one window open at a time (each manager yields to the other). Left: every correspondent — existing letters first (even writers who have died: the letters remain, the road closed), then the freshest bonds, with courier/whereabouts detail lines. Right: the whole correspondence as letter cards parsed from letters.txt (Core `CorrespondenceLog.Parse`, unit-tested — writer, timestamp, provenance, body; asides like "read and let lie unanswered" as soft narration), a courier still on the road noted at the end, and a composer with the story open alongside — single-line input with a tall wrapped draft mirror ("Seal and send"; this IS the proper letter-writing screen the encyclopedia task wanted) on the same QueueLetter road as the courier menu, same rules (one courier per bond, the near are pointed to speak, the dead cannot answer). "Write back" on an arriving letter now opens the window on that thread (popup fallback when it cannot). AND letters now show as letters inside the chat window's thread (the "letters between the messages" half of the ask): compose beats and received-letter beats are recognized by markers (`PromptBuilder.IsComposeLetterBeat` / `TryExtractReceivedLetter`, unit-tested against the shipped Angel templates) and rendered as "✉ by letter" cards in their place between the spoken messages, instead of raw quill-instruction narration. Config: `EnableLetterWindow`, `LetterWindowHotkey`. 169 Core tests green. (2026.07.11 00.25.00)
- [x] Quick-wins window batch + notice-click face-to-face (six V1 asks in one pass, 2026.07.11). (1) Chat window shows the whole circle, not just the room: `NearbyHeroesForChat` now also lists every remembered bond who is away across the map (walks the campaign folders like the letter window does), tagged "(here)"/"(away)" beside the name (sea-glass vs cool grey), here-first ordering; the send is grayed for the away and a gentle line points to a letter — `ChatContactInfo.IsHere`, `ChatContactVM.PresenceText/PresenceColor`, `ChatWindowVM.IsAway/AwayNotice`, `CanSend` requires the chosen one be here. (2) Persistent drafts: both windows keep the half-written message/letter when closed and restore it on reselect — session-scoped draft stores in `ChatWindowManager` (per NPC stringId) and `LetterWindowManager` (per correspondent folder), saved on each keystroke, cleared on send. (3) Socialness stepper gained [0] and [24] rails flanking the −/+ (bar widened 264→348, new `ExecuteMin`/`ExecuteMax`). (4) Relation points show beside the name in both windows and update as an exchange moves them — new `ImmersiveChatBehavior.RelationValue`/`RelationLabel` ("+23 · warm"), green/red/parchment tint, recomputed in each VM's `RefreshSelectionState`. (5) Auto-scroll: when the draft mirror appears/folds in the chat window the thread re-pins to bottom so the NPC's last line stays visible above the box (`RequestScrollToBottom` on the HasDraft transition). (6) Notice-click opens the OLD-STYLE face-to-face conversation, no accept/decline: reach-outs already speak first and record the greeting (`DeliverFirstWordAsync`); the greeting now rides on the parked `PendingNotice`, and clicking it runs new `OpenReachOutFaceToFace` (mirrors the accept path minus a second greeting — sets `_pendingInitiation`/`_recapReady`, shows the recorded line, falls into the talk loop). X'ing the notice fabricates nothing — the recorded greeting's [place,time] stamps let her see the silence. New config `OpenInitiationsFaceToFace` (default on) takes precedence over `SendInitiationsToChatWindow` for what the click does; `SpeaksFirstOnInitiation` routes both speak-first shapes through `DeliverFirstWordAsync`. Build clean (0 errors), deployed. Not yet playtested. (2026.07.11 11.48.14)

- [x] Night interactions — the world sleeps: NPC face-to-face reach-outs are damped at night so no one crosses a dark camp at three in the morning. New Core `InitiationScorer.NightFactor(hourOfDay)` — undamped through the day (`DawnHour` 06:00–`DuskHour` 22:00), then the group hourly chance is divided by a factor rising on a raised-cosine trough from 1 at dusk/dawn to `DeepestNightDivisor` (8) at the night's middle (~02:00), passing /2 in the shallow night. Raised cosine keeps it continuous at the day's edges (no cliff at 22:00 that snaps evening chats shut). Wired into `PickInitiatingNpcForThisHour` (× `NightFactor(CampaignTime.Now.CurrentHourInDay)`) and surfaced in the odds inspector ("the world sleeps — reach-outs at ×N of their daytime chance right now"). FACE-TO-FACE only: letters are untouched (a distant hand's writing hour is never seen, only the arrival days later). `CampaignTime.CurrentHourInDay` verified public on this game version via ilspycmd. Unit-tested (full-by-day, /8 deepest, ~/2 shallow, continuity at the dusk edge, wrapping). Build clean, deployed. Not yet playtested. (2026.07.11 12.34.09)

- [x] Personal goals — each NPC now carries their own personal aims, what they strive for of their own will (win back a lost hall, see a child wed, be free of a lord), held in a `goals.txt` beside `self.txt` and folded into the prompt as "What you strive for" right after "Who you have become". New Core `NpcGoals` (a list like `KnownFacts`, general to the NPC like `NpcSelf`) with fuzzy add/drop/revise (exact → containment → word-overlap match, conservative so a miss is safer than a wrong drop) + `SetAll` replace-all. Two shaping paths, exactly as asked: (1) a `tend_goals` native tool (`Tools\GoalTool`, add/drop/revise, one aim per call) rides EVERY spoken path beside `move_heart` (`CompleteSpokenAsync`), used sparingly — the whisper tells them to reach for it only when what they strive for genuinely shifts, unlike the every-reply heart; tool-capable backends only (`CanTendGoals`), a change earns a soft "sets an aim in order…" activity notice. (2) reflection reworks them wholesale — a new `GOALS:` section in `BuildReflectionRequest`/`ParseResponse` with the same replace/none contract as `FACTS:` (`ReflectAsync` gains `NpcGoals`/`maxGoals`), so "gathering their thoughts" resettles their aims on any backend. `NpcPersona.Goals`/`CanTendGoals`, `NpcPaths.GoalsFile`/README, `EnableNpcGoals` + `MaxNpcGoals` (default on, cap 6, clamp 1..20), `LoadGoals`/`SaveGoals` (one per line, `#`/`//` comments ignored). Unit-tested (NpcGoals ops + fuzzy match, GOALS parse/replace, prompt rendering + whisper). Build clean, deployed. Not yet playtested. (2026.07.11 12.34.09)

- [x] Socialness [24]-jump → [+2] coarse step — the socialness stepper's far-right rail was a one-click jump straight to the max of 24; Anton found that overwhelming (more than 3-4 visits/day is too much), so it is now a coarse **+2** step up instead. `SocialnessVM.ExecuteMax` (→24) replaced by `ExecuteIncreaseBig` (Snap + `BigStep` 2.0, capped at 24); the `[0]` rail (one-click "leave me be") stays as-is since an instant off is legitimate. Prefab `ImmersiveSocialness.xml`: the button's Text "24"→"+2" and Command.Click rebound. Build clean, deployed. Not yet playtested. (2026.07.11 12.40.32)

- [x] Mod Config in the Mod menu (MCM export) — the mod's settings are now editable in-game through the Mod Configuration Menu (MCM v5), a SOFT dependency: if a player doesn't have MCM, the mod runs on config.json exactly as before (McmBridge checks the MCMv5 assembly is loaded before ever touching an MCM type, and the MCM-facing code is JIT-quarantined behind that check). config.json remains the single source of truth — MCM is a live two-way editor: on startup config values are pushed into the menu, and every menu change is written straight back into the one shared ModConfig instance and its file, so no menu default can ever wipe a value set by hand (an existing API key from a prior version is safe). New files: `src/ImmersiveAI.Module/Mcm/ImmersiveAiMcmSettings.cs` (the AttributeGlobalSettings<T> with the exposed subset, grouped Connection / Windows & Hotkeys / Life of the NPCs / Advanced, with tooltips) + `Mcm/McmBridge.cs` (the soft-dep glue). Exposed (Anton's requested set + the feature toggles a Steam player should discover): Backend, Anthropic/OpenAI API keys + model dropdowns, MaxTokens, EnableChatWindow, chat/letter window hotkeys (dropdowns of map-safe keys, RequireRestart), NotifyWhenReplyReady, EnableNpcInitiatedChats, Socialness (DailyInitiationRate float slider), EnableLetters, EnableWorldRecall, EnableWebSearch, MaxKnownFacts, MaxNpcGoals, DevMode. Model/hotkey dropdowns add-if-missing so a hand-set value still shows. SubModule loads one shared ModConfig for the whole process and binds MCM at OnBeforeInitialModuleScreenSetAsRoot (retried at OnGameStart). Referenced MCMv5.dll with Private=false (never shipped in our bin — its module provides it), McmBinFolder prop in Directory.Build.props, optional DependedModuleMetadata "Bannerlord.MBOptionScreen" (LoadBeforeThis, optional=true) in SubModule.xml. Built + deployed clean (no MCM DLL in our bin). Not yet playtested in-game. (2026.07.11 14.30.00)

- [x] LLM health check when entering a game — a quiet "are you there?" ping the moment a campaign is loaded, so a player with a missing key, a wrong key, or no connection learns it at once from plain, actionable guidance instead of discovering a mute mod only when they try to speak with someone. New `LlmHealthCheck` (Module, `RunOnce`, fired from `SubModule.OnGameStart` after the behavior is added): runs off-thread, at most once per process (the fix for any failure is edit-config-and-restart, which re-runs the check). Missing key is caught before any call — "no <backend> API key is set … add your key to <config path> and restart the game" (amber). Otherwise a tiny 16-token "reply OK" ping (30s timeout) confirms the pipe: on success a soft sea-glass "connected to <backend · model>. The world is listening.", on failure a classified sentence, each ending in "restart the game": network/timeout/socket → "could not reach the AI service — check your internet connection"; 401/403 or "api key" → "the AI service rejected your API key … check the key in <config>"; 429 → rate-limit/out-of-credit; 404 or "model" → unrecognized model; 5xx/529 → service having trouble, wait; anything else surfaces the raw (truncated) message + the config path. Uses the existing `ChatClientFactory`/`MainThreadDispatcher`, no Core changes. Build clean, deployed. Not yet playtested. (2026.07.11 15.10.00)

- [x] Save-dependent memories + memories survive a rename — the ask was to keep an NPC's memories when a name changes, and to let reloading a save rewind their memories too. **Player rename was already safe**: the campaign folder's id is minted once (with the player's first name baked in) and frozen inside the save via SyncData, never re-derived from the live name — so renaming the player character never re-points the campaign folder. The real gap was the **NPC folder**, `<stringId>_<FirstName>`, which `NpcPaths.NpcFolder` resolved from the live hero every call — so renaming an NPC (a companion, an appearance/rename change) shifted the path and orphaned `memories.json`/`self.txt`/`goals.txt`/`letters.txt`. Fixed: `NpcFolder(npcId, firstName)` now resolves by IDENTITY. When the name-labelled path is missing it scans the campaign folder for a folder belonging to the same stringId — a folder named exactly the id matches unambiguously; a `<id>_<suffix>` folder is only claimed when the `NpcId` recorded inside its `memories.json` actually equals this id (`NpcIdInFolder` + `Sanitize` compare), so a shorter id (lord_1) can never steal a longer id's folder (lord_1_2_Alice) — then it heals the folder to the current name via `Directory.Move` (best-effort; if locked, it uses the old folder in place and retries next load). NpcId is reliably present because `JsonMemoryStore.SaveTo` throws on an empty id and the main path saves `npc.StringId`. Brand-new NPCs (the common case) find nothing and fall straight through to the new folder — the scan only runs on the miss path, so there's no steady-state cost. Known limit: a rename that happened BEFORE this shipped (an already-created empty new-name folder shadowing the old one) isn't auto-merged, and the reflection-baked `Summary`/`KnownFacts` are unaffected either way. **Reload-divergence** (loading an earlier save keeps "memories from the future") — Anton reversed the earlier "leave it" and asked for it: let a reload truly un-remember a bad turn ("it's a game, a little piece of paradise — let them revert"). NOT done via the old partial CampaignTime-drop (can't un-bake compressed Summary/KnownFacts); done as a **full per-save memory snapshot**. New `MemorySnapshotStore` (Module) photographs the whole campaign memory folder into `_snapshots\<token>\` and restores it on load, so everything (memories/self/goals/letters/letters-on-the-road, incl. compressed facts) rewinds exactly. The save↔photo tie: a GUID token minted into each save via `SyncData` (`IsSaving`), read back on load — sidesteps needing the load-time save name (the engine doesn't expose it cleanly). `CampaignEvents.OnSaveOverEvent` (an `IMbEvent<bool,string>`; `OnSaveOver` is NOT overridable on CampaignBehaviorBase) writes the photo when the save completes and uses the save NAME only to prune that slot's previous photo (overwrite reuses disk); `_index.json` maps save→token; oldest pruned past `MaxMemorySnapshots` (40) + orphan folders GC'd. Restore in `OnGameLoaded` after campaign-id resolve + migration, before any chat/letter read; fail-safe (empty/missing photo restores NOTHING — never blanks live memory). Config `RevertMemoriesWithSaves` (default on) exposed in MCM ("Memories rewind with your saves"). Legacy/pre-feature saves carry no token → safe no-op (old behavior). Known limits: reverts hand-edits too; the no-token era still bleeds as before (no regression). `NpcPaths` README + CLAUDE.md runtime-files updated. Module-only, build clean, deployed. Not yet playtested. (2026.07.11 16.45.00)

- [x] Mood swings — the passing weather of the heart: every NPC now carries a daily humor, and the women of the world also keep their body's own monthly season, so the same soul meets the player differently day to day and the interplay of humor × season × scene varies the encounters. New Core `MoodTides` (Prompts, pure + unit-tested): a 16-phrase humor palette ("in bright spirits…", "weary in your bones…", "brooding…", each completing "This day finds you …"), and for women a four-turning cycle exactly as Anton's research laid out — the custom days 1–5 (low strength, tears near the surface, misted thoughts), the rising days (strength returning, clear head, sociable), the crest of 3 days mid-cycle (bold, warm, full of life), and the waning days (feelings closer to the skin, quicker to stir/bruise/flare, comfort calls) — each woman on her OWN calendar (length 26–30 days + phase offset seeded from her id, hers for life). She is told her turning plainly so she can weigh it in her decisions, in reverent old-world words ("the custom of women", Gen. 31:35 phrasing — never a clinician's chart), and two days of three the season biases the humor pick toward its cluster so the two interplay. EVERYTHING is deterministic — FNV-1a over (StringId, campaign day), no state, no persistence, no rolls — so the same day always finds them the same and a reload rerolls no one's weather (stable across net472/net8 unlike string.GetHashCode). Module: `SituationBuilder.BuildMood` folds the paragraph in right after the self (before troubles/tidings), so it reaches every path — live chat, chat window, reach-out desire questions, letters, the situation file, and the prompt inspector; the cycle is given only to women 15–50 and not with child (pregnancy is already narrated in the self). Config: `EnableMoodSwings` + `EnableWomensCycle` (both default on; the cycle can be turned off alone). 7 new Core tests (determinism, palette variety, cycle rails 26–30, phase walk/shape 5-custom/3-crest, narration marks, empty-id). Build clean, 189 tests green, deployed. Not yet playtested. (2026.07.11 18.32.00)

- [x] The NPC is the narrator — the full prompt sheet reworked into the NPC's own FIRST PERSON (was: the long second-person Angel narration; Anton's playtest read of Thyrsif's full_prompt_snapshot, 2026.07.11). The sheet now reads "I am Thyrsif… / A Nord noble of clan Erking, sworn to Nord — a woman of some 35 years (gender+age moved up into the persona head) / My traits are… / When I speak, it comes out like this:… / My kin and house, close to me:… / Who I have become: / My goals are: / About Calradia: / About me: / What Vulgrim is to me (as I last gathered my thoughts on …): / Truths I decided to hold: / How should I speak:". Deep memory MOVED to sit right before the arrival: SituationBuilder joins setting and THE MOMENT with `PromptBuilder.MeetingSeparator` (`[[the-moment]]`) and BuildSystemPrompt splits there — scene → memory → truths → "And now Vulgrim, my husband, comes to me." — the separator never reaches the LLM and shows as `· · ·` in current_situation_info.txt. The player's kinship is spoken at the arrival (FamilyBuilder.KinshipTo: my husband/wife/father/mother/son/daughter/brother/sister/the head of my clan/my liege/my kinsman) so no one wonders who their own family is. FamilyBuilder first person + clan members now carry what they are about ("Yfinja (leading a warband of her own)", "Stohrith (governor of X)", "riding with Y") and the clan head wears their kinship ("led by my husband Vulgrim"); DescribeSelf slimmed to how I presently STAND (governor/pregnant/prisoner/party+its named duty via EffectiveScout/Surgeon/Engineer/Quartermaster — "I serve as its scout" — /army/siege), identity+spouse+children deduped away (persona head + family already carry them). MoodTides, TidingsFormatter ("…reached my ears", "I have overheard…"), TroubleBuilder ("this is how I tell it"), DescribeOther ("Where my heart stands toward them", "Our peoples — … — are at war") all first person. Guidance shortened for freedom (Anton: long rules make them all speak the same): BrevityGuidance + OldWorldToneGuidance tightened, all four gift whispers recast short and first person, the recall whisper now INVITES the unbidden look ("I may look unbidden too — at my company, at the one before me, at the world — to open with something real"). ModConfig defaults recast (AtmosphereLine "I am {name}…", RoleplayGuidance short first person) with Normalize migrating configs still carrying the old defaults verbatim (hand-edited lines honored). The Angel remains a real person in the DIALOGUE beats (desire questions, arrivals, letters, reflection) — recorded Angel turns replay as spoken forever, templates untouched. Raw-prompt dump dividers "────────" → "--- x ---" (parsed badly). Tests updated + new split-ordering test. Build clean, 193 green, deployed. Not yet playtested. (2026.07.11 19.05.00)

- [x] Tools used more freely + the truths' hand + real search queries — the tool layer pushed toward Anton's "not only when instructed": (1) `seek_wisdom` reframed from "the far-seeing sages" (retired — Anton disliked the framing) to searching "all I have ever read and heard"; description now commands "look before you ever invent or plead ignorance"; result text tightened, closing fourth-wall defense kept. (2) The immersed search query is now SHARPENED by a small refining LLM call before hitting DuckDuckGo: `RefineSearchQueryAsync` (behavior, plain CompleteAsync — no tools) sees the last incoming words of the exchange (`LastIncomingWords`, tail of the newest user message) and returns one effective "Mount and Blade Bannerlord …" query (or plain-English for `beyond`); any failure falls back to the old game-name-prepended raw question — so "how might I grant my ships…" stops searching for nonsense. (3) NEW `hold_truth` tool (`Tools\TruthTool` + Core `NpcMemory.AddKnownFact`/`DropKnownFact`, unit-tested): mid-reply the NPC sets down (or releases) ONE lasting truth into KnownFacts — dedupe, MaxKnownFacts honored with an honest "your mind holds all it can" answer, fuzzy release (exact then containment, miss-safe). Applied to the LIVE NpcMemory the turn speaks from (`CompleteSpokenAsync` gained `liveMemory`; ExecutePlayerTurnAsync passes it) so the end-of-turn save can't clobber it; saved immediately either way. (4) Activity notices on the left for both personal hands: "X sets an aim in order…" (existed) and NEW "X sets down a truth to keep…"/"X releases a truth once held…" (`ResolveTruthEdit`, ShowNpcActivity-gated). New whisper line rides only when the tool does (`NpcPersona.CanHoldTruths`). No new config — hold_truth rides whenever the backend carries tools; reflection still rewrites the whole truths list. Build clean, 193 green, deployed. Not yet playtested. (2026.07.11 19.05.00)

- [x] Playtest-batch fixes (Anton's Thyrsif snapshot + the Asgotha talk, 2026.07.12): (1) **Dangling encounter after forced map talks** — clicking a reach-out / approach opened `CampaignMapConversation.OpenConversation` hung on the NPC's MOBILE party, which spins up a PlayerEncounter that nobody finished, dumping the player into the engage-party menu ("Attack / Send troops / Leave") against their own kin on exit. Fixed: `OpenConversationWith` (now instance) notes whether an encounter existed BEFORE opening (`_finishEncounterAfterTalk = PlayerEncounter.Current == null`), and `OnConversationEnded` finishes only the one our talk created — next tick via the dispatcher so the conversation fully unwinds — never touching a pre-existing town encounter. (2) **Hearts stuck at 0 on gpt-4o** — Asgotha's whole warm conversation logged FeltShift 0 throughout: gpt-4o has gone shy of volunteering `move_heart` again (11 tools now ride). Fixed as a HYBRID: `HeartTool.Tally.Weighed` marks a turn whose heart was truly weighed (any readable number, including an honest 0 — set in `ResolveHeartShift`); `ExecutePlayerTurnAsync` respects a weighed heart as before, but when the tool never came it falls back to the separate Angel feeling question (the pre-tool path, still in place) — so the player always gets feedback, honest 0s are never asked twice, and no inline marks return (settled). Tool description + whisper sharpened: "Let the measure agree with your words: warmth spoken and a 0 set down cannot both be true", "this measures the moment, not the room left on a scale". (3) **Asgotha invented castle advice instead of searching** — "advice for new city owners" slipped past the "how a thing is done" trigger; `seek_wisdom` description + whisper broadened to counsel/advice on ruling towns and castles etc., "even when my own wit could half-answer: the looking makes my counsel true, where invention would make it hollow". (4) **Menja twice in the kin block** — clan-people roll now excludes close kin already named (spouse/children/parents/siblings), and age-0 children read "(a babe in arms)" not "(a girl of 0)". (5) **"Vulgrim, the head of my clan" for her own husband** — the polygamy mod leaves her Spouse slot pointing elsewhere, so KinshipTo gained a shared-child rule: a common child names them "the father/mother of my children" — true in any marriage arrangement. (6) **Expecting mothers get their own body-season** — Thyrsif (pregnant) had no season line by design; now `MoodTides.WithChildSentence` gives 4 deterministic carrying-day variants ("the child within me stirs…", quiet-and-strong, hungering, tiring) + a with-child humor cluster (tender/weary/warm/wistful/hearthsick, 2 days of 3), and the palette grew to 20 humors (stern, quietly merry, longing-for-the-hearth, open-handed) wired into the cycle clusters. Gated by `EnableWomensCycle` like the monthly season; monthly + carrying never both. Build clean, 194 tests green, deployed. Not yet playtested. (2026.07.12 00.55.00)
- [x] BUG - can move camera angle. The socialness control's Gauntlet layer held a permanent mouse claim (`SetInputRestrictions(true, Mouse)`); the map hides the cursor for right-drag rotation, the claim forced it back, and the view snapped home after an inch. Fixed with a per-tick hover gate in `SocialnessManager.Tick`: the layer claims the mouse only while `GauntletLayer.HitTest()` says the cursor is over a button (everything else in the prefab is DoNotAcceptEvents), and releases it otherwise — camera, clicks, and the stepper all whole. (2026.07.12 01.14.15)
- [x] I can read the text of the letters that have not yet arrived. Two leaks closed: letters.txt used to receive an NPC's letter at QUEUE time (the letter window is a view over letters.txt, so the words were readable while the courier rode) — now `Letter.Logged` (default true, so bags persisted before the flag never double-log) defers the log entry to DELIVERY, dead writers' folders resolved by identity; and the chat window's "✉ by letter" card used to render the recorded compose beat at once — an in-flight letter now shows as "has written you a letter — it is sealed, and rides toward you still" (`IsLetterOnRoadToPlayer`, matched by writer + body against the live LetterBag). (2026.07.12 01.14.15)
- [x] Make sure NPCs on the move can send letters too (parties/caravans of mine). Three real causes fixed: `SituationBuilder.Place`/`PlaceDescription` fell back to `Settlement.CurrentSettlement` — the PLAYER's settlement — so a distant writer's letter claimed the wrong letterhead (now only someone truly WITH the player borrows it; a moving party writes from "the road"); the recency decay in `InitiationScorer.Pull` silenced anyone away for weeks (one's own clan — companions leading parties and caravans, kin, governors — now carries duty floors: `Pull(..., inPlayersService)` floors recency at 0.6 and closeness at 0.5, Core-tested); and their compose line now invites a field report ("your company and its state, the road behind you, battles fought" — `ComposeLetterLine(inService)`, marker-safe). (2026.07.12 01.14.15)
- [x] More roles immersion — the whole wave, shipped as "the company and the crafts" (2026.07.12, the night shift). What landed:
    Party roles: every duty-holder knows their charge in the situation ("As its scout, the road, the pace of the march… are mine to judge; my eyes are able at the craft" — scout/surgeon/engineer/quartermaster, each weighed by their REAL skill via `CraftsBuilder.WordFor`), a leader knows who holds his duties ("In my company, X is my scout…"), and the chat window tags your own party's contacts "rides with you — your scout".
    The field-craft tools (`Tools\FieldCraft`, ride with the recall channel ONLY for souls with a party on the map): `survey_surroundings` — every band moving within sight (kind, faction, strength counted as well as the asker's Scouting honestly can, foe or friend, distance in rider's words, and who is the swifter — the true answer to "can we escape them?"), plus our own pace and what drags it (the game's real `SpeedExplained` lines); `weigh_battle` — our company (or whole army) against a named band, army, or walled place (garrison + militia at half weight), compositions from real rosters (foot/bowmen/horse/horse-archers), verdict worded by the true strength ratio and confidence by the asker's Tactics. Surgeon's ledger folded into `recall_company`: how fast the named and the ranks mend, under whose care (the game's own PartyHealingModel).
    Crafts for everyone: `CraftsBuilder` weighs real skills into honest words ("What my hands and wits are honestly good at: masterly in Medicine; able in Scouting…") on every persona sheet — wanderers can truly answer "what would you be good at?", and `recall_person` now lists a looked-up soul's strongest crafts (so a tavern-keeper asked about a wanderer answers from truth).
    Wives/family: children are named WITH their other parent ("My children — with Vulgrim: Menja (a girl of 12)…", polygamy-safe), grown children carry where life has taken them, and when the spouse is the one arriving the situation says plainly that between wedded souls there is no ceremony — tenderness, the children, and the grand designs of the house are all one conversation.
    Occupations: artisans, tavern-keepers, ransom brokers, smiths, arena masters, headmen, merchants, and caravan masters each carry one first-person trade-knowledge sentence (an anchor to reason from — the rest they work out or seek_wisdom), and caravan leaders speak of their rounds and ledgers, not "warbands".
    Nobles/renown: the beholder's eye — when a great lord (2+ clan tiers above, or a crowned head 1+) is met by a near-stranger (standing < 10), the situation smashes what his eyes would see into one sentence: garb by real armor tier, the best blade by tier, banner or none, the following at their heels, "no word of their deeds has ever reached me" when renown is low — and leaves the welcome to his own nature, no rudeness hard-coded. Reach-outs: a stranger far above the player's station carries a smaller stranger-floor (king → 0.2×), so kings don't cross rooms for unknowns; real bonds are never dampened by rank.
    (2026.07.12 01.14.15)

- [x] Info sections in the chat and letter windows — a "?" button now sits beside the X in both the O (chat) and U (letter) windows, unfolding a full-window help overlay: what the window is, who is listed and what the tags mean, how it works (Enter/Escape, drafts kept, hearts moving, sealed letters, one courier per bond, "Seal and send" being deliberate), and a WHAT TO TRY section with concrete example asks (surgeon/scout/merchant/lord; far-off companions writing field reports). Texts live in the VMs (`InfoText`, hotkey names pulled live from config so a rebound key reads true), the overlay is the same scroll-panel pattern as the deep-memory block, Escape folds the overlay first and only a second press closes the window (both managers), Enter-to-send is suppressed while the help is open, and `LetterWindowVM` now takes the ModConfig. Build clean, 197 tests green, deployed. Not yet playtested. (2026.07.12 09.27.13)
- [x] discuss with Claude — Fable's V1-readiness proposals, settled: Anton's one reaction confirmed — the "panic switch" IS covered by socialness 0 (verified in code: reach-outs roll `DailyInitiationRate x unionPull / 24` and letters roll `WriteRateFactor x DailyInitiationRate x ...`, so rate 0 silences both; the only autonomous thing left is an NPC answering a letter the player themselves sent, which is invited, not autonomous — so the proposal is struck, no new toggle needed). The remaining proposals were promoted to first-class V1 tasks in TASKS_TODO.md (first-run key guide, graceful key-death, cost visibility, prompt-files story, performance index, localization decision = ship English-only and say so, ConfigVersion stamp), joined by four new pre-release items from the same pass: privacy/provider disclosure on the Steam page, a diagnostics log file for triaging player reports, a release-defaults + uninstall-safety once-over (the SaveDefiner InformationData question), and a reproducible Workshop packaging step. (2026.07.12 09.27.13)

- [x] The V1 pre-release batch (Anton: "I like your points, please do them" + Costs expanded + models rethink) — nine tasks shipped in one wave, all Module-side, 197 tests green, deployed:
    COSTS EXPANDED + COST VISIBILITY (merged): `UsageLedger` (Module) — both chat clients now parse the usage object the APIs already return and report every call's real tokens; an AsyncLocal interaction scope (`BeginInteraction`) aggregates a whole flow (spoken reply + tool rounds + feeling question + compression) into ONE soft slate-gray notice: "✒ Name — message: 2,431 → 156 tokens, 3 calls, ~$0.014". Kinds: message/greeting/approach/first word/memory work; the SPOILER flows (reaching-out desire, letter compose, letter reply) bill quiet — log + totals only — so a cost line never reveals a private weighing or breaks a sealed letter. Prices from a new `ModelPrices` config dict (USD per MTok, longest-key match like ModelContextWindows, defaults verified 2026.07: opus-4-8 5/25, sonnet 3/15, haiku 1/5, gpt-5.6 sol/terra/luna 5/30 2.5/15 1/6, gpt-4o 2.5/10 …); unknown model = tokens shown, no price. Config `ShowCostNotices` (default on) + `MaxDailyRequests` (default 0 = off) — the daily counter persists in usage.json so the cap survives restarts; at the cap the clients throw a plain explanation and the autonomous rolls skip. The odds view now ends with the session/day cost summary line.
    GRACEFUL KEY-DEATH: `LlmGate` (Module) — the clients report every failure with its HTTP status (401/403 → 30 min quiet, 429 → 15, 5xx/network → 5); while quiet the hourly reach-out and letter rolls skip entirely (no more hammering a dead key, no wall of red), the player is told ONCE in plain words, direct speech still tries at once, and any success reopens everything.
    DIAGNOSTICS LOG: `ModLog` (Module) — rolling log.txt beside config.json (512KB, one log_old.txt generation): health check results, client failures, gate transitions, every cost line, conversation errors. What a Steam bug report pastes.
    FIRST-RUN KEY GUIDE: `FirstRunGuide` (Module) — a keyless campaign entry now raises ONE kind popup (where keys come from, the exact config path and field, restart note, MCM mention), marker-file once-per-install (first_run_note_shown.txt; delete to re-see), raised from the health check's no-key branch.
    PERFORMANCE GUARD: `MemoryIndex` (Module) — path-keyed cache of (NpcId, name, richness, lastTalkDay) per memories.json, self-invalidating on the file's write stamp+length (so saves, snapshot restores, and hand-edits need no wiring); MaybeStartNpcLetter, CoLocatedPull, and the odds view all read it instead of re-parsing every file each hour — 300-NPC campaigns stop paying I/O per hour.
    CONFIGVERSION: `"ConfigVersion": 1` stamped into config.json (missing deserializes 0 → normalized to 1), so V1.1 migrations have a number to key off.
    PROMPT-FILES STORY: global_prompt.txt's auto-created template rewritten as a real "how to shape your world" guide with four commented ready-to-uncomment examples + pointer to per-NPC custom_instructions.txt; the Steam-page section drafted.
    WORKSHOP PACKAGING: tools/package.ps1 — clean-slate dist\ImmersiveAI layout (same contents as deploy) + version-stamped zip read from SubModule.xml (tested: v0.1.0); dist/ gitignored.
    STEAM PAGE DRAFT: docs/steam-page-draft.md — full description draft with the [REQUIRED] disclosures: privacy (conversations go to YOUR provider under YOUR key; web search → DuckDuckGo; zero telemetry), costs ("what an evening costs" + the in-game notices), Steam AI-content disclosure, uninstall-mid-campaign note with the [VERIFY] placeholder, clean-room provenance, prompt-files section, English-UI/any-language-replies line.
    MODELS RETHINK (Anton's "GPT-5.6 Terra?" — confirmed): docs/models-and-costs.md. OpenAI default gpt-4o → gpt-5.6-terra ($2.50/$15, 1M ctx, GPT-5.5-class at half price); existing configs deliberately NOT migrated (a model swap is the player's money). New `OpenAIReasoningEffort` config (default "low" — a little reasoning makes tool use reliable, none is cheapest) and the CRITICAL client fix: gpt-5.x/o-series get `max_completion_tokens` + `reasoning_effort` instead of `max_tokens` (without it Terra is a hard 400; gpt-4o keeps the classic shape). Anthropic default stays claude-opus-4-8; ModelContextWindows gained gpt-5.6 (1M) + current Claude entries. Utility-model split (luna/haiku for the small calls) parked as a POST-V1 task with the ledger to justify it.
    Also settled by the drafts rather than code: privacy & provider disclosure (page), localization decision (English UI, say it; NPCs answer in any language — proudly). Not yet playtested. (2026.07.12 09.56.56)

- [x] BUG: map-party talk + Immersive AI section ends in the engage menu ("Attack / Send troops / Leave") — Anton's Jarlinna repro made it exact: vanilla farewell fine, OUR farewell bug, even with nothing said. Cause: a talk begun by clicking a party on the map lives inside a real PlayerEncounter; vanilla's leave lines set PlayerEncounter.LeaveEncounter so the encounter dissolves with the words, and our close_window lines (Farewell + the two test-parting levers) never did — so the closing conversation dropped the player into the engage menu against their own friend. Fix: RequestLeaveFromPartyEncounter as the consequence on every close_window line of ours — sets LeaveEncounter only when a PlayerEncounter exists AND the player is not in a settlement (requesting leave inside a town would walk them out of the gates for ending a chat). Complements the 2026.07.12 00:55 fix (that one finishes encounters OUR forced talks created; this one releases encounters the PLAYER's click created). Constraint for future dialog work: every new close_window exit line must carry this consequence. The already-stuck save heals by simply choosing Leave in the menu once. (2026.07.12 10.44.12)
- [x] gpt-5.6 + tools on chat completions — two live-fire fixes the same hour, both caught by log.txt: (1) reasoning models spend their THINKING against max_completion_tokens, so the 16-token health-check ping (and any tight budget) died with 400 "Could not finish the message"; EffectiveMaxTokens(effort) now adds effort-scaled headroom (+512 low … +2048 high) on top of MaxTokens, which keeps meaning the SPOKEN budget. (2) OpenAI's /v1/chat/completions REFUSES function tools + reasoning_effort together on gpt-5.6 ("… use /v1/responses or set reasoning_effort to 'none'") — every tool-carrying call now rides at 'none' automatically while plain calls (feeling number, desires, search refining) keep the configured effort; config/MCM hints updated to say so honestly, /v1/responses migration parked as POST V1. Also: Diagnose classifies both new failure shapes ("insufficient permissions" = valid key without model access — NOT a bad key; "max_tokens or model output limit" = budget eaten by thinking) with actionable messages. (2026.07.12 10.44.12)

- [x] Intermittent OpenAI 401 "insufficient permissions" after granting gpt-5.6 access — the log showed the same request shape succeeding and failing seconds apart (greeting OK 10:46, advice round-1 401 10:47, round-1 OK + round-2 401 within one second 10:49): OpenAI's model-access change still PROPAGATING through their fleet, not a config problem. Fix: the OpenAI client now retries exactly this error once after 1.5s (PostOnceAsync returns status+body so the send can be repeated; a truly bad key fails twice and reports as before; the retry is logged). Encounter bug confirmed FIXED by Anton the same session. (2026.07.12 10.51.47)
- [x] gpt-5.6-luna the OpenAI default; MCM dropdown slimmed to luna/terra/sol — Anton's call: the whole 5.6 tier is sure-handed with the NPCs' tools, so the cheap one ($1/$6 per MTok) wins the default for the fast-use player; the older models (gpt-5.5/5.1/5/4.1/4o) are markedly worse with the NPCs and left OUT of the mod's menu — the tinkerer sets them by hand in config.json, and McmBridge's SelectOrAdd still shows a hand-set model in the dropdown so nothing silently breaks. Changed: ModConfig default, OpenAIChatClient blank-model fallback, the MCM dropdown + hint, docs/models-and-costs.md + CLAUDE.md. Existing configs stay unmigrated as before (a model swap changes real money and voice). (2026.07.12 12.30.00)
- [x] Chat window role tag + duty roleplay handle TWO-role companions — one soul can hold several party duties (scout AND surgeon…), but SituationBuilder.PartyDuty returned only the first match: the O-window tag showed one role, the situation sheet gave her only one duty's meaning, and a leader's "In my company" could name the same person twice. Now PartyDuties returns them all: the tag reads "rides with you — your scout and surgeon", the sheet says "I serve as its scout and surgeon" AND carries a DutySentence per duty (each weighed with its own craft-word), and HeldDuties groups by holder ("Alandra is my scout and surgeon"). PartyDuty kept as the joined-label convenience. (2026.07.12 12.30.00)
- [x] BUG: typing in the encyclopedia's search box opened the O/U windows — the encyclopedia is an overlay that never changes the GameState (still MapState), so the raw hotkey polls saw "o"/"u" keystrokes meant for the search field. Fix: new UI\MapOverlays.IsEncyclopediaOpen reads SandBox.View.Map.MapScreen.Instance.EncyclopediaScreenManager.IsEncyclopediaOpen — the very flag vanilla gates its own escape menu on — by CACHED SOFT REFLECTION (SandBox.View stays unreferenced; a future game reshape degrades to the old behavior instead of failing the mod), and both ChatWindowManager.CanOpenNow and LetterWindowManager.CanOpenNow check it. (2026.07.12 13.05.00)
- [x] Keep access gates the chat widget — Anton's fresh-start find: the guards demand a bribe to enter the keep, yet the O window happily reached the nobles inside. New IsBehindClosedDoors (behind IsCoLocated in the behavior): anyone the settlement's LocationComplex places in "lordshall" or "prison" is only co-located if the player may truly pass that door — SettlementAccessModel.CanMainHeroEnterLordsHall (the very model behind the "pay a bribe to enter the keep" menu) grants FullAccess, OR the limited-access solution is a bribe and Settlement.BribePaid already covers BribeCalculationModel.GetBribeToEnterLordsHall (vanilla's own paid-bribe rule). Effects fall out of IsCoLocated everywhere: no chat, no reach-outs from behind the door; a known keep-dweller shows "(away)" in the window with send grayed pointing to a letter — which still finds them, the road into the keep words cannot take. Tavern folk, notables, streets unaffected; fail-open on any model hiccup. (2026.07.12 13.40.00)
- [x] The player's TRUE station in every NPC's eyes — no more "noble" at clan tier 0 (Anton's fresh-start find: the game stamps the player Occupation "Lord" from day one). SituationBuilder.DescribeOther now names the player what they truly are via PlayerStation: sworn to a crown as a vassal = nobleman/noblewoman (kingdom's own leader = crowned king/queen); under a mercenary contract = sellsword captain; unsworn and free = free adventurer / free captain of a small band (15+) / free captain at the head of a warband (40+). The house line grew tier-honest too (HouseLine + ClanStandingWords, tier 0 "a banner newly raised, its name not yet known to anyone" up through 6+ "among the greatest houses of Calradia") and says "riding under contract in the pay of X" for mercs instead of "sworn to X". recall_person got the same truth for the player only (WorldRecall.PersonRole: free adventurer/captain sworn to no crown, sellsword captain under contract) — real NPC lords keep their noble titles. (2026.07.12 14.05.00)
- [x] BUG: Cadugan, headman of an iron village, said Beglomuar specializes in grain — traced in his memories.json + the persona code: the headman/rural-notable trade-knowledge line HARD-CODED "its fields and herds", so he faithfully roleplayed farmland he does not have; and recall_place carried no village production at all, so the tool could not have corrected him. Fix both from the game's own truth (Village.VillageType): PersonaBuilder.VillageLivelihood appends "Our life and bread is the iron we send to market, beside some ..." (PrimaryProduction + up to two secondary Productions) to the headman's sheet, and recall_place now recalls "Its livelihood is iron, beside some ..." for any village. (2026.07.12 14.35.00)
- [x] Two Cadfin-chat finds (his grain answer itself was the already-fixed village-livelihood bug, pre-fix build): (1) "clan fen Teicolar, sworn to fen Teicolar" — an unsworn clan IS its own MapFaction, so the Facts kingdom fallback named the clan as its own liege; now dropped when it equals the clan name (fixes NPC minor factions too). (2) Asked "do you need any work?", a notable with NO game issue invented quest-shaped work; TroubleBuilder now gives issue-less notables one honest line — "No true trouble weighs on me… nothing worth hiring a fighting company for; small everyday labor, paid in kind" — so improvised fence-mending flavor stays welcome but never masquerades as a real hire. (2026.07.12 15.00.00)
- [x] The Brunda 401s — OpenAI's "insufficient permissions" STILL flickering hours after the model-access grant (log: exchanges succeeding and failing minutes apart, one 1.5s retry losing whole replies). Two answers: (1) the propagation retry grew to three attempts with rising pauses (1.5s/3s/4.5s) — a truly bad key still fails them all and reports as before; (2) the proven gpt-5.5 family returned to the MCM dropdown as the stable fallback — gpt-5.5-mini ($0.25/$2) SECOND after luna per Anton's ask, then terra/sol/gpt-5.5/nano — with explicit ModelPrices entries (5.5-mini/nano would otherwise longest-match "gpt-5" and bill wrong); IsReasoningFamily already covers them via the gpt-5 prefix. (2026.07.12 15.40.00)
- [x] Model-family correction (Anton's research): there ARE no gpt-5.5 mini/nano — the small tier jumped from 5.4 straight to the 5.6 luna/terra. Dropdown now: luna, gpt-5.4-mini (the stable fallback, second per Anton), terra, sol, gpt-5.5, gpt-5.4, gpt-5.4-nano. Prices tuned to the real table: gpt-5.5 corrected to $5/$30 (flagship, was wrongly $1.25/$10), gpt-5.4 $2.50/$15, 5.4-mini $0.75/$4.50, 5.4-nano $0.20/$1.25 (explicit entries — they'd otherwise longest-match bare "gpt-5" and bill wrong); context windows: 5.4/5.5 full = 1M, 5.4 mini/nano = 400k. Anton's config.json verified clean of the short-lived phantom 5.5-mini entries (never launched with that build), so the corrected defaults merge in on next load. (2026.07.12 16.00.00)
- [x] gpt-5.4-mini crowned the OpenAI default — settled by Anton's live play (the day's third revision: terra -> luna -> 5.4-mini): luna's voice was fine but the access-propagation 401s kept biting, while 5.4-mini ran without a hiccup at 3/4 the price. ModConfig default, client blank-model fallback, dropdown order (5.4-mini first, luna second) + hint, docs/models-and-costs.md (decision story + cost-table row) and CLAUDE.md updated. (2026.07.12 16.20.00)
- [x] BUG: city stopped answering map clicks after Anton left it (terrain clicks fine) — the Brunda wedge. Not the keep gate (read-only): a reach-out NOTICE lives 2 game days and never re-checked distance, so clicking it after riding out had OpenConversationWith hang the talk on the DISTANT town's own party — the engine spins up a half-real settlement encounter that never dissolves, and an encounter-shadowed settlement ignores clicks. Four defenses: (1) IsNoticeStillAlive now requires IsCoLocated — the knock cannot outlive the parting, the notice lapses on separation (either side moving); (2) OnMapNoticeInspected re-checks at click time — parted = honest toast + the chat window on her thread (the greeting is already recorded), never a forced distant face-to-face; (3) OpenConversationWith hangs on a settlement party ONLY when the player truly stands in that settlement, else the player's own party carries the scene; (4) a conservative hourly self-heal finishes a PlayerEncounter seen NAKED (no menu, no mission, no talk, no settlement, no map event) on TWO consecutive ticks — frees an already-wedged save within two game hours, logged to log.txt. (2026.07.12 16.50.00)
- [x] Two Brunda-quartermaster finds: (1) she could not see the company's silver — recall_company's coin ledger was leader-only; now the party's EffectiveQuartermaster also keeps the books ("the war chest — {leader}'s purse — holds some N denars"), honest to her charge. (2) markdown clutter — replies arrived dressed in **bold**, dash-lists, and whole-reply quotation marks on a panel that renders nothing; new PromptBuilder.PlainSpeechGuidance rides "How should I speak" on every sheet, told in-world ("I speak my words aloud; they are heard, not read from a page... no marks of the pen... a new line for a new thought is all the shape my speech needs") — new lines stay allowed, the emote-asterisk grammar of the dates plan will carve its own exception when it lands. 197 tests green. (2026.07.12 17.20.00)
- [x] *Acting out* — the emote grammar lands (the V1 must-have): NPCs are now invited to act out small gestures between single *asterisks* (*I pour the wine and slide the cup across*), set apart from their spoken words, as the ONE exception carved out of PlainSpeechGuidance (PromptBuilder.ActingOutGuidance, placed right after the plain-speech rule because it IS that rule's exception). Sparing by its own wording — one act, rarely two, always brief, "my words carry the scene, never a stage-play of directions" — the convention cuts both ways (the player's *offered arm* was done, not said), and a gesture weighs what the heart has earned (a stranger's touch and an old friend's are different acts — the dates-plan insight shipped early). The chat window draws gestures as soft gray narration between the spoken cards: Core EmoteText splits bodies on a deliberately strict grammar (single asterisks hugging content, no newlines, ** and space-padded spans stay literal so markdown residue and 2*3 math never get swallowed), ChatWindowVM.AddSpoken renders segments in order with the header on the first so an all-gesture reply still says whose act it was; letters and Angel beats untouched; face-to-face panel shows the classic literal *starred* convention. Config: EnableActingOut (default on) -> NpcPersona.EncourageActingOut. 207 tests green (10 new). (2026.07.12 15.45.00)
- [x] Release-defaults once-over + uninstall safety VERIFIED — the last hands-on pass before upload: DevMode ships false (all test levers gated behind it), DailyInitiationRate 0.3, every player-facing default sane, MCM soft-dep verified (SubModule.xml optional="true"; McmBridge quarantines all MCM contact behind a loaded-assembly check + NoInlining, so an absent MCM never resolves a type). The uninstall question answered by decompiling the game itself (v1.4.7 TaleWorlds.SaveSystem + CampaignSystem): a save carrying a pending portrait notice LOADS FINE with the mod removed — the whole load path tolerates the unknown type id (CreateObject leaves it null, resolve/fill/callbacks all null-check and skip, the List element lands as null) and CampaignInformationManager.OnGameLoaded itself scrubs null notices (RemoveAll(t => t == null || !t.IsValid())) before re-showing; the player only sees the standard "different modules" warning. Registration still matters at save-WRITE time with the mod installed — the definer stays forever, comment in ImmersiveChatMapNotification.cs corrected to the verified truth. One belt-and-braces in-game confirmation left on the playtest checklist. (2026.07.12 22.05.51)
- [x] Workshop description finalized from docs/steam-page-draft.md — [VERSION] filled (Bannerlord v1.4.7, read from Native's SubModule.xml) and the [VERIFY] uninstall placeholder resolved into a plain positive note ("Safe to remove at any time... the game quietly discards the notice... reinstall later and every character remembers you") on the strength of the save-system decompilation above. The AI-content disclosure tick stays on the upload-day checklist (it lives in Steam's form, not the text). Assets (screenshots + clip) remain Anton's. (2026.07.12 22.05.51)
- [x] V1 playtest checklist written — docs/playtest-checklist-v1.md gathers every in-game item that needs Anton's hands into one document with pass criteria per box: the roles wave (scout "can we escape them?", weigh_battle vs a castle, wife on the children, king receiving a tier-0 stranger, caravan field-report letter), the "?" overlays + letter window end-to-end, the morning batch (cost notices, sealed-flow silence, odds cost line, log.txt, first-run popup, key-death quieting, gpt-5.6 on OpenAI), the farewell fix (map + town), the one-line uninstall confirmation, and the Anthropic key test. TASKS_TODO collapsed to point at it. (2026.07.12 22.05.51)
- [x] Letter window key moved U -> Y — War Sails binds "U" to the ship manager at sea, so pressing it there opened the ship screen instead of the letters (the raw hotkey poll cannot outrank a vanilla binding; a different key is the only clean fix, as Anton suspected). Default is now "Y" (right beside U, free on the campaign map, already in the MCM curated list), carried by the first real ConfigVersion migration: Normalize's V2 step moves a config still sitting on the old default "U" to "Y" and stamps ConfigVersion 2 — any other hand-picked key is honored untouched. Fallbacks in LetterWindowManager.ParseKey / ChatWindowVM / LetterWindowVM follow; MCM dropdown default follows; the "?" overlays already read the key live from config so no text was stale. Docs swept (CLAUDE/AGENTS/README + both steam page files). (2026.07.15 12.05.50)
- [x] Acts sometimes walked unmarked — NPCs narrated what they DID bare in their spoken lines instead of between *asterisks*, so the chat window read the act as speech. PromptBuilder.ActingOutGuidance firmed: the invitation's "may ride" became "rides", plus one new clause making the mark the act's only home — "every act I make takes it, and never do I tell an act bare among my spoken lines as though it were speech". The sparing-use wording, the cuts-both-ways convention, and the heart-weighing line all stand as they were; the test-pinned "between single asterisks" phrase and the placement right after PlainSpeechGuidance unchanged. 210 tests green. (2026.07.15 12.05.50)
- [x] NPCs now see the player's own house — wed to whom (the full list: polygamy mods like Marry Anyone park further wives in ExSpouses while both live on wed, so the single Spouse slot alone was never the truth), father/mother of which children (grouped by the other parent, ages and grown-lives shown, like the NPC's own kin block), and whose child they are — new FamilyBuilder.DescribeFamilyOf(person, beholder) folded into DescribeOther in the meeting block; the beholder reads "wed to me", not her own name as a stranger's. Checked the other half of the ask: NPCs already knew their OWN family (FamilyBuilder: parents, spouse, children with ages, siblings, clan) — and while there the polygamy gap in their self-knowledge closed too: the NPC's own spouse line now lists every living spouse, KinshipTo + the "no ceremony between wedded souls" closeness now recognize a living ExSpouse as a true wife/husband (Thyrsif greets Vulgrim as "my husband", not "the father of my children"), and co-wives stay out of the clan roll. (2026.07.15 14.21.42)
- [x] "Write back" on an arrived letter now truly opens the letter WINDOW — the old single next-tick try raced the closing arrival inquiry (IsAnyInquiryActive was still true one tick later), so CanOpenNow said no and the flow silently fell back to the small popup composer. LetterWindowManager.OpenWhenClear parks the ask and retries every tick (~2s worth) until the map is clear; the popup composer fires only when the window truly cannot come up (window disabled, mission, timeout). (2026.07.15 14.21.42)
- [x] V1 GOES PUBLIC — release day: the Y-key Steam sweep done (description on the final BBCode, change notes on the upload); v1.0.0 packaged (package.ps1) and uploaded to Workshop item 3764210301 via the official TaleWorlds.MountAndBlade.SteamWorkshop.exe (NOT SteamCMD — rides the logged-in Steam client, no credentials; the whole 3-step loop written down in tools/WORKSHOP-UPLOAD.md, ChangeNotes live in tools/WorkshopUpdate.xml); visibility flipped PUBLIC — ranks #1 for "Immersive AI" in Workshop search, Stohrith's thumbnail beside ChatAi; Nexus page live (nexusmods.com/mountandblade2bannerlord/mods/12119, page text in docs/nexus-page.bbcode.txt with upload cheat-sheet); announcement posted (draft + posting plan in docs/reddit-announcement.md — r/Bannerlord karma-gated us, so r/mountandblade/forums first, r/Bannerlord after karma). Remaining: watch comments, answer fast. (2026.07.15 22.47.44)
- [x] Custom OpenAI-compatible endpoint (OpenRouter / NanoGPT / local servers) — asked for on Nexus (Prost25prost25, 2026.07.16), the first post-release request shipped. New `OpenAIBaseUrl` config key (default the real OpenAI, so existing users are untouched byte-for-byte): Normalize completes a pasted base URL ending in /v1 to the full /chat/completions path (missing scheme becomes https, "openrouter.ai/api/v1" just works); OpenAIChatClient takes the endpoint via ctor (factory passes it). Request shape: bare model id on any endpoint keeps today's exact logic (max_completion_tokens + reasoning_effort none for the gpt-5/o families); a provider-prefixed router id ("openai/gpt-5.4-mini", the OpenRouter/NanoGPT convention) gets the universal shape — classic max_tokens (routers translate per provider) plus the routers' own `reasoning: {enabled:false}` so a routed gpt-5.x cannot think its spoken budget away (the "..." bug's vaccine). Health check names a custom host ("connected to OpenAI-compatible (openrouter.ai) · model"); MCM gains a "Custom endpoint (advanced)" text field (blank = real OpenAI, two-way bridged); ModelPrices/ModelContextWindows match router ids by containment for free. Custom model ids are set in config.json (SelectOrAdd surfaces them in the dropdown). Normalization rules proven by an 8-case script; build clean, 210 tests green. NOT live-tested against OpenRouter/NanoGPT (no key) — the health check is the player's instant verdict. (2026.07.16 12.40.00)
- [x] OpenRouter as a FIRST-CLASS backend (Anton's ask after testing the custom endpoint by hand — "make it easy to work with from the Mod config"): Backend gains "OpenRouter" beside Anthropic/OpenAI — own `OpenRouterApiKey` + `OpenRouterModel` (default openai/gpt-5.4-mini), fixed `ModConfig.OpenRouterEndpoint`, same OpenAIChatClient with a new providerLabel so every error/log line says "OpenRouter", OpenRouter attribution headers (HTTP-Referer/X-Title) sent ONLY when the endpoint host is openrouter.ai. MCM: Backend dropdown 3-way, OpenRouter key field + curated model dropdown (ids verified against the LIVE /models catalog — Claude slugs are DOTTED there: anthropic/claude-haiku-4.5, claude-opus-4.8; openai/gpt-5.4-mini first, both families in), custom-endpoint field now points OpenRouter users to the real backend. Health check three-way via SelectBackend (custom-host naming kept for the OpenAI backend), FirstRunGuide names openrouter.ai + the right key field, MemoryTokenProfile resolves OpenRouterModel; price/context tables need NO new rows (containment: "claude-haiku"⊂"anthropic/claude-haiku-4.5", "claude-opus-4"⊂"...opus-4.8"). LIVE-TESTED with Anton's real key before writing the code: plain reply + reasoning:{enabled:false} accepted, native tool call CAME (recall_person("Derthert")), Claude-via-router OK — the router path is proven, not presumed. Also: MCM hint texts trimmed under ~320 chars (they overflowed the tooltip box, Anton's screenshot), Anton's config.json migrated by hand to Backend=OpenRouter with his key in the right field. Folded into v1.1.0 (changelog/ChangeNotes/pages/FAQ all reworded to lead with OpenRouter; dist zip repacked). 210 tests green. (2026.07.16 14.10.00)
- [x] The OpenRouter dropdown widens to six families — Google and the rest (Anton's ask): google/gemini-2.5-flash + gemini-3.5-flash, deepseek/deepseek-v4-flash (cheapest tool-capable model going, $0.10/$0.20), x-ai/grok-4.5, mistralai/mistral-large-2512 join the GPT/Claude picks — every id filtered by the catalog's supported_parameters "tools" AND live-tested with a real tool-call prompt (all called recall_person). The find of the day: "Reasoning is mandatory for this endpoint and cannot be disabled" — fable-5, grok-4.5 and gemini-3.5 all hard-400 on reasoning:{enabled:false} through OpenRouter (our own shipped claude-fable-5 dropdown entry was broken!); fixed with a self-healing retry in OpenAIChatClient.SendAsync (400 + that message → strip the reasoning field, retry once, let the model think as it must — no per-model list to maintain), verified working for all three. Price rows (gemini-2.5/3.5-flash, deepseek-v4-flash, grok-4.5, mistral-large-2512, from the live catalog) + context rows (gemini 1M, grok 256k). LESSON burned into the session: never round-trip the UTF-8 bbcode docs through PowerShell Get/Set-Content on PS 5.1 — it double-encoded every em-dash (restored from git, re-applied via the Edit tool). 210 tests green, deployed, dist repacked. (2026.07.16 15.05.00)
