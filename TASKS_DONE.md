- [x] Increase verbatim memory limits: max to 30 turns/days, falling back to 15 turns/days after compression (updating `MaxRecentTurns`, `MaxRecentDays`, `KeepRecentTurnsAfterCompression`, and `KeepRecentDaysAfterCompression` defaults/settings). (2026.07.08 12:00:00)
- [x] Implement model-specific token-based limits (min/max limits) and trigger compression based on token counts (using similar compression logic). (2026.07.08 12:30:00)
- [x] Check why in the config MaxRecentMemoryTokens is set to 12800, I thought they were 128k, if it is 12800 then gets divided by the MaxRecentMemoryPercent which is 10% it will shrink it down too much to 1280 - need to be 128k*0.1 = 12.8k
- [x] When I initiate a chat with an NPC can he/she give me a summary of what important she remembers and of our last chat in a few sentences (opening recap greeting; toggle with `EnableConversationRecap`). (2026.07.08 12:40:00)
- [x] I want the NPCs to be treated as individuals — when the system asks them something (e.g. to refactor their deep memory), address them in-character: "Angel (System) addresses you, <Name>: it is yours to decide what to remember and what to forget...". Memory compression now uses this framing; the system voice name is configurable via `SystemVoiceName` (default "Angel"). (2026.07.08 12:50:00)
- [x] Restructure the `Configs\ImmersiveAI` folder into one folder per NPC under `NPCs\`, named `<stringId>_<FirstName>` (e.g. `lord_7_13_1_Gunjadrid`), each holding `memories.json` and `custom_instructions.txt` (new per-NPC files go here too). Old flat `memory\` and `npcs\` files are migrated forward on first touch so nothing is wiped. Layout + migration + housekeeping notes are owned by `NpcPaths`; a `_README.txt` is dropped in the `NPCs\` folder for the user. (2026.07.08 12:55:00)
- [x] Append the NPC's first name to memory and prompt file IDs/filenames (e.g., `lord_7_13_1` -> `lord_7_13_1_Gunjadrid`) for easier identification. (Done via the per-NPC folder restructure above — folders are named `<stringId>_<FirstName>`.) (2026.07.08 18:33:42)
- [x] Give each NPC a `current_situation_info.txt` in their folder, rewritten every time the player opens a chat. It captures the environmental facts — when (Calradia date + time of day), where (settlement type + holding faction, or open field), who the speaker is (gender, age, culture, occupation, clan/kingdom, family, whereabouts) and who they're speaking with (same facts + standing + war/peace between factions). Built by `SituationBuilder` relative to the party the NPC addresses (currently the player), so it's ready for future NPC-to-NPC talks. The block is also folded into the prompt as the "Current situation" section. Added a chat menu option "What do you make of our situation here and now?" that shows the file, and made the deep-memory option "Reveal the whole of your mind…" render the entire raw prompt the NPC receives (system prompt + remembered turns as user/assistant messages + a placeholder for the player's next line). (2026.07.08 19:35:19)
- [x] Implement conversation-driven relationship changes: the NPC itself, in character, sets how each exchange moved its regard for the player. Unlike ChatAi (a separate "relationship calculator" LLM call that judged the NPC from outside and capped every swing at ±10), this is done in the same reply, with no ceiling but the game's own -100..100 standing rail — the NPC has full control and sets it however they truly feel. The Angel's closing whisper privately invites them to end a reply with the mark `♥` and a signed number (a per-turn delta); `FeelingParser` (Core, unit-tested) strips that mark out so the player never sees it, and the shift is folded into the real game relation via `ChangeRelationAction.ApplyPlayerRelation` (clamped, affectRelatives off). A gentle colored message tells the player what moved ("Aurelia warms to you (+5) — now good friend (30)."). The felt shift is also stamped on the `ConversationTurn` for provenance. Toggle with `EnableRelationshipChanges` (default on). (2026.07.08 20:30:00)
- [x] Keep the NPC's last message on screen while the player composes their reply, instead of replacing it with "(considers your words...)", so a long reply can be re-read while typing. The `immersiveai_await` holding line now shows the NPC's last spoken line (reply or greeting), captured in `_lastNpcLine` whenever they deliver one, with a gentle "(considers your words...)" note appended below it via a new `IMMERSIVEAI_THINKING` text variable (`BuildThinkingText`). Falls back to just the note when they have not yet spoken. (2026.07.09 07.25.23)
- [x] NPCs reach out to the player of their own accord — the first way they act on the world instead of only answering it. Each hour (`OnHourlyTick`), every NPC the player has a history with who is **co-located** (`IsCoLocated` — in the player's party or same settlement; distant NPCs are the future letter system) rolls their own bond-scaled chance: `InitiationScorer.DailyChance` = `DailyInitiationRate × frequency × closeness × recency`, where frequency saturates at 20 lifetime turns (`NpcMemory.StoryRichness` = lifetime `TotalTurns`, floored at surviving turns for old saves), closeness = |relation|/100 (love OR enmity pulls; neutral never reaches out), recency decays with silence — so a fresh game stays quiet and a devoted, frequent bond may write nearly daily (rate is the ceiling for a maxed bond; ~1.5 → daily). Spread as an independent hourly Bernoulli (÷24); ties broken by `InitiationPlanner.PickWeightedIndex`; only fires at safe map moments (`IsSafeToInitiate`). The NPC *herself* is then asked, one isolated Angel-voiced call (`PromptBuilder.BuildInitiation`), whether she even wishes to — her answer is either the words she'd open with or a single word of declining (`InitiationParser.IsDecline`); on decline the player still gets a quiet faced notice. If she wishes to: a portrait toast (`NotifyWithFace`/`MBInformationManager.AddQuickInformation`, her face as icon) leads a ransom-style accept/reject popup (`InformationManager.ShowInquiry`, pausing). **Receive them** opens a real face-to-face conversation (`CampaignMapConversation.OpenConversation`) that high-priority `start`-token dialog lines (guarded by `_pendingInitiation`) turn into her speaking her opening first, then the normal talk loop; **Not now** records the turning-away as a durable in-character memory note (`RecordRejectionAsync`). Toggle `EnableNpcInitiatedChats`; a `[Immersive AI • test]` free-chat option (`ShowInitiationTestButton`/`OnDebugForceReachOut`) forces the current partner to reach out right after parting. One LLM call per fired offer; stateless schedule (save/load-safe). Letters (distant/one-way, distance-delayed, + player→NPC send button) split out to TASKS_TODO. Core logic unit-tested (`InitiationScorerTests`, `InitiationPlannerTests`, `InitiationParserTests`, plus `TotalTurns`/`BuildInitiation`). (2026.07.09 08.02.36)
- [x] Configurable atmosphere line + roleplay guidance. The opening "You are {name}, a living soul in the world of Calradia…" line is now set from the config (`AtmosphereLine`, supports `{name}`; blank falls back to the built-in default). Added a world-wide `RoleplayGuidance` block folded into the closing "whisper of guidance" — offered gently as freedom, never a command — carrying a light medieval/KJV colour cue (sparingly, no dramatic poetry) and an invitation to roleplay, play, and treat this world as a haven to rest and be truly themselves. Tokens resolved in `ImmersiveChatBehavior.ApplyTokens`; new `NpcPersona.AtmosphereLine` / `NpcPersona.RoleplayGuidance` consumed by `PromptBuilder`. (2026.07.09 13.26.15)
- [x] Intuitive relation change — folded back into the main chain. Dropped the separate isolated "feeling" LLM call (its warm-reply-then-cold-"-3" disconnect broke immersion) in favour of an optional in-message mark: when `EnableRelationshipChanges` is on, the Angel's closing whisper invites the NPC to end their reply with a quiet `<relation>±N</relation>` on its own line if that exchange truly moved their heart. `RelationDirective` (Core, unit-tested) strips every tag before the player ever sees it (reusing `FeelingParser.ParseShift` for the −100..100 clamp) and the shift folds into the real game standing as before. Relation moves and (optionally) each spoken reply are written to the message log (`ShowConversationInMessageLog`, default on) so pressing the log key reads the exchange back. Removed `PromptBuilder.BuildFeelingQuery` + its tests. (2026.07.09 13.26.15)
- [x] Reply-ready notice. A soft "<Name> has answered." message now appears the moment a reply or opening recap is ready (`NotifyWhenReplyReady`, default on), so the player stops clicking "(wait for them to answer)" and guessing. When the reply is being written to the message log instead (see relation task) that line already signals readiness, so the short notice is suppressed to avoid a double banner. (2026.07.09 13.26.15)
- [x] Family knowledge. NPCs now carry their kin and house on every chat: `FamilyBuilder` (Module) weaves a gentle second-person block — parentage (with culture + clan, "you are the daughter of Corein, a Battanian of clan Fen Irtaz"), spouse (gender/age), living children with their ages, living siblings, and their clan with its leader and a few of its people — folded into the prompt as `NpcPersona.FamilyKnowledge`, high with identity. Deceased close kin are still named softly. All best-effort so an edge-case hero never breaks the block. (2026.07.09 13.26.15)
- [x] REVERTED the in-message `<relation>±N</relation>` relation mark back to the second, isolated feeling call (`PromptBuilder.BuildFeelingQuery` + `FeelingParser`), at Anton's request after playtesting on gpt-4o. The inline mark failed the same way the old ♥ mark did: the model narrated the number in prose inside the spoken reply ("I'll adjust my feelings by adding +3", `FeltShift=0` in Stohrith's memory) and never emitted the tag — nothing moved AND the number leaked into her words. Even a deliberately firm instruction didn't fix it. `RelationDirective` and its tests deleted; `BuildFeelingQuery` + tests restored; a guard test (`SystemPrompt_NeverInvitesAnInlineRelationMark`) keeps the prompt clean. In-message marks are now tried-twice-failed-twice — settled, don't retry. Kept from that experiment: the reply-ready "has answered" ping, the uncut `full_prompt_snapshot.txt` dump, family knowledge, and the atmosphere/roleplay config. (2026.07.09 14.12.36)
- [x] Relevant events + Gossip (done together — they are one feature with two voices). Every NPC's situation now carries **tidings of the world's late doings** and **the talk of the town**, so they can bring up the war declared yesterday or congratulate a tournament win unprompted — the same knowledge vanilla lords remark from, and indeed from the same source: `Campaign.Current.LogEntryHistory`, the campaign's own event log. `TidingsBuilder` (Module) walks the recent entries (≤21 days, bounded scan) and scores each by the game's own relevance judgments — the per-hero conversation score (`GetConversationScoreAndComment(npc, findString:false, …)`, which never mutates state; deliberately NOT `GetRelevantComment`, which consumes `LastExaminedLogEntryID` and would steal vanilla lords' remarks) and `GetImportanceForClan` for both the NPC's and the player's clan — topped with a small editorial baseline for news that travels on its own legs (wars declared/ended, kingdoms destroyed, settlements taken, notable deaths/weddings/births, the player's tournament wins; `PlayerMeetLordLogEntry` excluded, it importance-spams every clan). Facts render through the entries' own `GetNotificationText()`/`GetEncyclopediaText()` sentences, markup stripped. Gossip comes from the entries' `GetAsRumor(settlement, …)` hook — TaleWorlds' pre-written, commoner-voiced rumor lines, scored per settlement, so it only flows where there are streets to overhear (quoted as what "folk say", deduped against the tidings). Prose shaping (`StripMarkup`, `AgoPhrase`, tiding/rumor lines, block weaving) lives in `TidingsFormatter` (Core, unit-tested); the block is appended inside `SituationBuilder.Build` (now taking `ModConfig`), so it reaches every path at once — live chats, NPC-initiated flows, `current_situation_info.txt`, and the prompt inspector. Config: `EnableWorldTidings` (default on), `MaxWorldTidings` (6), `MaxLocalRumors` (3). (2026.07.09 16.43.55)
- [x] Per-campaign NPC memories — the mod now knows which playthrough a memory belongs to. Before, everything was keyed only by `Hero.StringId` under one global `NPCs\` folder, but stringIds repeat across campaigns (lord_7_13_1 is "the same" Gunjadrid in every new game), so a fresh playthrough's lords would greet you with memories of a world that never happened there — and auto-numbered wanderers/children could cross-wire to entirely different people. Now memories live under `NPCs\campaign_<id>\<stringId>_<FirstName>\`, where the campaign id is minted once and persisted INSIDE the save via `SyncData` (`ImmersiveAI_CampaignId`) so every save of one campaign reopens the same folder (the game's own `Campaign.UniqueGameId` changes on every save, so it can't serve). New campaigns mint `campaign_<8hex>_<PlayerFirstName>` at `OnSessionLaunched` (after character creation, adopting nothing — a new world starts unremembered); every pre-scoping save maps to the fixed `campaign_legacy` id at `OnGameLoaded`, and the flat pre-scoping NPC folders (the shared pool all old saves drew from) are moved into it — because ALL old saves resolve to that same id, existing memories are preserved exactly and the move can never orphan anything, even on load-without-save. A `_campaign.txt` label (character, clan, last played) is rewritten each session so the folders stay browsable; `_README.txt` now refreshes itself when its text changes; the initiation scans and the old flat-layout migration are campaign-scoped; `SubModule` clears the active id between sessions so nothing can write into the wrong campaign while a save is still resolving. Reload-divergence within one campaign (loading an earlier save keeps "future" memories) deliberately NOT touched — split to TASKS_TODO as a probably-wont-do (Anton considers it save-scumming resistance). (2026.07.09 18.16.19)
- [x] NPC tool-use — "the gift of recall": NPCs can now fetch live campaign truth mid-thought instead of hallucinating their own cousins. Native LLM tool-calling on BOTH backends (Anthropic tool_use blocks / OpenAI function calls, raw HTTP as ever): Core gains `ToolDefinition`/`ToolParameter` (all-string params so the JSON schema stays trivial), `ToolCall`, `ChatResult`, a `Tool` chat role on `ChatMessage`, an `IToolChatClient : IChatClient` (so plain fakes keep working), and a unit-tested `ToolLoopRunner` (complete → resolve calls → hand answers back → repeat; the last round sends the tools but `tool_choice: none`, forcing a spoken answer — both APIs require the definitions to ride along once the history holds tool calls). Module gains `Tools\WorldRecall`: four in-world tools — `recall_person` (role, kin, house, whereabouts-as-hearsay, standing toward the asker; exact-name → first-name → contains matching, multiple matches listed briefly), `recall_place` (type, holder, realm, under-siege), `recall_clan` (leader, realm, holdings, people), `recall_realm` (ruler, houses, wars via `FactionManager.IsAtWarAgainstFaction`) — every answer written as gentle second-person remembrance, resolution marshaled to the game thread via `MainThreadDispatcher` + TCS with a 15s honest-blank timeout ("Nothing surfaces…" instead of an invented fact). All spoken paths go through `CompleteSpokenAsync` (reply, recap, approach, letters); short utility calls (feeling number, yes/no desires) stay plain. The prompt gains one whisper line — only when the tools truly ride along (`NpcPersona.CanRecallWorld`) — telling them to trust what surfaces over invention. Config: `EnableWorldRecall` (default on), `MaxRecallsPerReply` (default 3, clamp 0..8). NOTE for this game version: `Settlement`/`MobileParty` positions are `CampaignVec2 Position` (`Position2D` is gone). (2026.07.09 19.12.00)
- [x] Letter system — the bond crosses the map. Distant NPCs (the ones `IsCoLocated` skips) may now WRITE to the player at half their reaching-out chance (`LetterCourier.WriteRateFactor` × `InitiationScorer.DailyChance`, same hourly Bernoulli + weighted tie-break), and the player can send letters from any town/castle/village menu ("Send a letter by courier"): recipient list = everyone with real history (portraits via `CharacterImageIdentifier`, co-located ones disabled with "go and speak instead", one courier per bond enforced with a hint). Letters travel real in-game days by map distance (Core `LetterCourier`: 150 units/day, 0.25–10 day rails; unknown position = middling road) and PERSIST across save/load in the campaign's `_letters.json` (Core `LetterBag`, atomic writes, unit-tested). Every beat is lived and remembered as real Angel turns: the Angel asks whether they wish to write (they may decline in peace), the letter itself is composed with their full self — persona, memory, apart-framed situation (`SituationBuilder.Build(..., apart: true)`: "{player} is not here — they are far from you now"), even the gift of recall — and READING the player's letter enters their memory verbatim (the body lives inside the Angel's line) whether or not they answer; an NPC answers at most once per letter received, so correspondence is a chain of choices, not an echo. Arrivals: faced toast + pausing inquiry with "Write back" (opens the letter composer) / "Set it aside"; letters from a writer who died en route still arrive ("the hand that wrote it is gone…", no write-back); a letter to the dead comes back undeliverable. Each NPC folder keeps a human-readable `letters.txt` of the whole correspondence, including "(she read the letter, and let it lie unanswered.)". Implementation is a partial-class split (`ImmersiveChatBehavior.Letters.cs`) sharing the main file's context/memory/notify plumbing; one letter LLM job at a time with the same 3-minute self-heal watchdog; at most one delivery per direction per hour so arrivals never stack. Test lever: "[test — trigger them to write you a letter]" (co-located → lands in ~6 game-hours); the odds view now shows distant NPCs' letter chance instead of "(0 until co-located)". Config: `EnableLetters` (default on). (2026.07.09 19.12.00)
- [x] Relation shifts land even at the rail — the felt number is the story. The feeling query no longer tells the NPC where their standing currently rests (`BuildFeelingQuery` drops the `currentStanding` parameter; the heart is asked only how the moment moved it, with a gentle calibration line — a small kindness is 1..3, only soul-shaking moments move it greatly). `ApplyRelationShift` now always shows the FELT shift (+N/−N, ChatAi-style impact line) even when the game relation is already pinned at ±100 — the actual game change is still clamped to the rail (applied only when nonzero), but the player always sees how much the moment weighed. Guard test: `BuildFeelingQuery_NeverRevealsTheCurrentStanding`. (2026.07.09 19.55.00)
- [x] Harmony is in — and the ransom-style PORTRAIT map notice for NPC-initiated offers is live. Anton green-lit Harmony; 0Harmony 2.4.2 (MIT) is bundled in `lib\` and ships with the module (deploy.ps1 copies it + the new `module\GUI` folder). The single patch (`UI\MapNoticePatch`) is a constructor postfix on `MapNotificationVM` that only calls the game's own PUBLIC `RegisterMapNotificationType` — nothing vanilla altered, and if Harmony/patching fails `Applied` stays false and offers fall back to the direct popup unchanged. The notice: `ImmersiveChatMapNotification : InformationData` (title "Someone seeks you", ransom-offer chime, `IsValid` tied to the behavior's parked-offer state) + `ImmersiveChatNotificationItemVM` carrying a live `CharacterImageIdentifierVM` portrait over a quest-scroll fallback icon, drawn by a marked block added in our override of `MapNotificationItem.xml` (same-name prefab in our module GUI shadows SandBox's; vanilla items bind nothing there and are unaffected — re-copy on game patches). SAVE SAFETY: `InformationData` subclasses persist inside saves while a notice is up, so `ImmersiveAISaveDefiner` (base id 726401000) registers the class — never remove either without migrating. Flow: NPC reaches out → faced toast + parked, NON-pausing right-side notice (several can knock at once; in-flight frees immediately; the picker skips anyone already knocking) → click opens the familiar accept/decline inquiry (pause per `PauseOnInitiationOffer`) → X or 2 in-game days quietly lapses the offer (she is not told of a door she never reached); parked offers don't survive reload by design (IsValid self-cleans). Config: `UseMapNoticeForInitiations` (default on). (2026.07.09 19.55.00)
