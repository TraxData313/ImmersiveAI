- [x] Increase verbatim memory limits: max to 30 turns/days, falling back to 15 turns/days after compression (updating `MaxRecentTurns`, `MaxRecentDays`, `KeepRecentTurnsAfterCompression`, and `KeepRecentDaysAfterCompression` defaults/settings). (2026.07.08 12:00:00)
- [x] Implement model-specific token-based limits (min/max limits) and trigger compression based on token counts (using similar compression logic). (2026.07.08 12:30:00)
- [x] Check why in the config MaxRecentMemoryTokens is set to 12800, I thought they were 128k, if it is 12800 then gets divided by the MaxRecentMemoryPercent which is 10% it will shrink it down too much to 1280 - need to be 128k*0.1 = 12.8k
- [x] When I initiate a chat with an NPC can he/she give me a summary of what important she remembers and of our last chat in a few sentences (opening recap greeting; toggle with `EnableConversationRecap`). (2026.07.08 12:40:00)
- [x] I want the NPCs to be treated as individuals — when the system asks them something (e.g. to refactor their deep memory), address them in-character: "Angel (System) addresses you, <Name>: it is yours to decide what to remember and what to forget...". Memory compression now uses this framing; the system voice name is configurable via `SystemVoiceName` (default "Angel"). (2026.07.08 12:50:00)
- [x] Restructure the `Configs\ImmersiveAI` folder into one folder per NPC under `NPCs\`, named `<stringId>_<FirstName>` (e.g. `lord_7_13_1_Gunjadrid`), each holding `memories.json` and `custom_instructions.txt` (new per-NPC files go here too). Old flat `memory\` and `npcs\` files are migrated forward on first touch so nothing is wiped. Layout + migration + housekeeping notes are owned by `NpcPaths`; a `_README.txt` is dropped in the `NPCs\` folder for the user. (2026.07.08 12:55:00)
- [x] Append the NPC's first name to memory and prompt file IDs/filenames (e.g., `lord_7_13_1` -> `lord_7_13_1_Gunjadrid`) for easier identification. (Done via the per-NPC folder restructure above — folders are named `<stringId>_<FirstName>`.) (2026.07.08 18:33:42)
- [x] Give each NPC a `current_situation_info.txt` in their folder, rewritten every time the player opens a chat. It captures the environmental facts — when (Calradia date + time of day), where (settlement type + holding faction, or open field), who the speaker is (gender, age, culture, occupation, clan/kingdom, family, whereabouts) and who they're speaking with (same facts + standing + war/peace between factions). Built by `SituationBuilder` relative to the party the NPC addresses (currently the player), so it's ready for future NPC-to-NPC talks. The block is also folded into the prompt as the "Current situation" section. Added a chat menu option "What do you make of our situation here and now?" that shows the file, and made the deep-memory option "Reveal the whole of your mind…" render the entire raw prompt the NPC receives (system prompt + remembered turns as user/assistant messages + a placeholder for the player's next line). (2026.07.08 19:35:19)
